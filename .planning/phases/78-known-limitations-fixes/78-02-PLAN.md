---
phase: 78-known-limitations-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kicad_agent/training/grpo.py
  - src/kicad_agent/training/__init__.py
  - src/kicad_agent/training/smoke_test.py
  - src/kicad_agent/placement/training/train.py
  - tests/test_grpo_rename.py
autonomous: true
requirements: [M-15]

must_haves:
  truths:
    - "GRPOConfig is renamed to AdvantageWeightedConfig with kl_coefficient and clip_range removed"
    - "GRPOTrainer is renamed to AdvantageWeightedTrainer with honest docstring"
    - "Backward-compatible aliases exist: GRPOConfig = AdvantageWeightedConfig, GRPOTrainer = AdvantageWeightedTrainer"
    - "All consumers (smoke_test.py, placement/training/train.py) import from the aliases without error"
    - "grpo_trainer.py GRPOLoopTrainer is completely unchanged"
    - "training/__init__.py docstring updated to reference new names"
  artifacts:
    - path: "src/kicad_agent/training/grpo.py"
      provides: "AdvantageWeightedConfig, AdvantageWeightedTrainer, backward-compat aliases"
      contains: "class AdvantageWeightedConfig"
    - path: "tests/test_grpo_rename.py"
      provides: "Rename verification tests"
      contains: "test_advantage_weighted_trainer_exists"
  key_links:
    - from: "src/kicad_agent/placement/training/train.py"
      to: "src/kicad_agent/training/grpo.py"
      via: "from kicad_agent.training.grpo import GRPOTrainer"
      pattern: "GRPOTrainer"
    - from: "src/kicad_agent/training/smoke_test.py"
      to: "src/kicad_agent/training/grpo.py"
      via: "from kicad_agent.training.grpo import GRPOConfig, GRPOTrainer"
      pattern: "GRPOConfig, GRPOTrainer"
---

<objective>
Rename GRPOTrainer/GRPOConfig in grpo.py to AdvantageWeightedTrainer/AdvantageWeightedConfig to accurately reflect the algorithm (advantage-weighted REINFORCE, not PPO/GRPO). Remove unused clip_range and kl_coefficient fields. Maintain backward-compatible aliases for all existing consumers (per D-M15, M-15, research recommendation: rename is correct because PPO ratio clipping is architecturally wrong for reward model training).

Purpose: grpo.py trains the REWARD MODEL via supervised BCE + advantage-weighted REINFORCE loss. The class names "GRPO" and the unused clip_range field are misleading. grpo_trainer.py (GRPOLoopTrainer) is already honest and uses advantage clipping correctly -- do NOT touch it.

Output: Honest class names, removed dead code, backward-compatible aliases, tests verifying rename.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-known-limitations-fixes/78-RESEARCH.md
@.planning/phases/78-known-limitations-fixes/78-CONTEXT.md

<interfaces>
From src/kicad_agent/training/grpo.py (current):
```python
@dataclass
class GRPOConfig:
    learning_rate: float = 1e-5
    group_size: int = 8
    kl_coefficient: float = 0.1      # UNUSED -- remove
    max_generation_length: int = 512
    temperature: float = 0.7
    seed: int = 42
    checkpoint_every: int = 100
    output_dir: str = "checkpoints/"
    lr_schedule: str = "cosine"
    warmup_steps: int = 100
    total_steps: int = 0
    # NOTE: clip_range was NOT in GRPOConfig -- it was in GRPOTrainingConfig (grpo_config.py)
    # RESEARCH CORRECTION: Verify if clip_range exists in GRPOConfig or only in grpo_config.py

class GRPOTrainer:
    def __init__(self, policy_model, reward_model, ref_model, config=None):
        ...
```

Consumers that import GRPOTrainer/GRPOConfig:
- src/kicad_agent/placement/training/train.py:98 -- `from kicad_agent.training.grpo import GRPOTrainer`
- src/kicad_agent/training/smoke_test.py:157 -- `from kicad_agent.training.grpo import GRPOConfig, GRPOTrainer`
- src/kicad_agent/training/__init__.py:17 -- docstring mentions "GRPOTrainer, GRPOConfig"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename GRPOConfig and GRPOTrainer in grpo.py with backward-compatible aliases</name>
  <files>src/kicad_agent/training/grpo.py, tests/test_grpo_rename.py</files>
  <read_first>
    - src/kicad_agent/training/grpo.py (FULL file -- contains class definitions, loss computation, all references to GRPOConfig/GRPOTrainer)
    - src/kicad_agent/training/grpo_config.py (if exists -- check if clip_range is in a separate config)
    - src/kicad_agent/placement/training/train.py (consumer that imports GRPOTrainer)
    - src/kicad_agent/training/smoke_test.py (consumer that imports GRPOConfig, GRPOTrainer)
    - src/kicad_agent/training/__init__.py (docstring references)
  </read_first>
  <behavior>
    - Test 1: `from kicad_agent.training.grpo import AdvantageWeightedConfig` succeeds
    - Test 2: `from kicad_agent.training.grpo import AdvantageWeightedTrainer` succeeds
    - Test 3: `from kicad_agent.training.grpo import GRPOConfig` succeeds (backward-compat alias)
    - Test 4: `from kicad_agent.training.grpo import GRPOTrainer` succeeds (backward-compat alias)
    - Test 5: AdvantageWeightedConfig does NOT have kl_coefficient attribute
    - Test 6: AdvantageWeightedConfig does NOT have clip_range attribute
    - Test 7: AdvantageWeightedTrainer.__init__ accepts config parameter of type AdvantageWeightedConfig
    - Test 8: `from kicad_agent.training.grpo_trainer import GRPOLoopTrainer` still works (unchanged module)
  </behavior>
  <action>
    In src/kicad_agent/training/grpo.py:

    1. Rename `GRPOConfig` to `AdvantageWeightedConfig`:
       - Remove `kl_coefficient: float = 0.1` field (unused -- computed but never applied to loss)
       - Remove `clip_range` if it exists (RESEARCH says it does NOT exist in GRPOConfig but does in grpo_config.py -- verify before removing)
       - Keep ALL other fields unchanged: learning_rate, group_size, max_generation_length, temperature, seed, checkpoint_every, output_dir, lr_schedule, warmup_steps, total_steps

    2. Rename `GRPOTrainer` to `AdvantageWeightedTrainer`:
       - Update type annotation: `config: AdvantageWeightedConfig | None = None`
       - Update internal reference: `self.config = config or AdvantageWeightedConfig()`
       - Update the class docstring to remove NOTE about misleading names (now honestly named)
       - Keep ALL methods and logic completely unchanged

    3. Add backward-compatible aliases at module level (AFTER class definitions):
       ```python
       # Backward-compatible aliases (deprecated, use new names)
       GRPOConfig = AdvantageWeightedConfig
       GRPOTrainer = AdvantageWeightedTrainer
       ```

    4. Update module docstring:
       - Replace Usage example to show new names:
         ```python
         config = AdvantageWeightedConfig(seed=42)
         trainer = AdvantageWeightedTrainer(policy_model, reward_model, ref_model, config)
         ```
       - Remove the NOTE paragraph about misleading names

    5. Do NOT modify any other files that import GRPOTrainer/GRPOConfig -- the aliases handle backward compatibility.

    6. Update src/kicad_agent/training/__init__.py docstring line 17:
       Change `- \`grpo\`: GRPOTrainer, GRPOConfig` to `- \`grpo\`: AdvantageWeightedTrainer, AdvantageWeightedConfig (aliases: GRPOTrainer, GRPOConfig)`

    7. Create tests/test_grpo_rename.py with 8+ tests covering all behaviors above. Tests should verify:
       - New names import correctly
       - Old names still work as aliases (type-wise: assert GRPOConfig is AdvantageWeightedConfig)
       - kl_coefficient removed from new config
       - grpo_trainer.py module unchanged
  </action>
  <verify>
    <automated>pytest tests/test_grpo_rename.py -x -v</automated>
  </verify>
  <done>
    AdvantageWeightedConfig and AdvantageWeightedTrainer are the primary names. kl_coefficient removed. GRPOConfig and GRPOTrainer exist as aliases. All consumer imports work unchanged. grpo_trainer.py untouched. Tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Code rename only, no behavioral changes, no external input. |

## STRIDE Threat Register

No STRIDE threats identified. This is a pure rename/refactor with no functional changes.
</threat_model>

<verification>
1. pytest tests/test_grpo_rename.py -x -v (rename tests pass)
2. python3 -c "from kicad_agent.training.grpo import AdvantageWeightedConfig, AdvantageWeightedTrainer, GRPOConfig, GRPOTrainer; print('OK')" (all imports work)
3. python3 -c "from kicad_agent.placement.training.train import PlacementTrainer; print('OK')" (consumer import works)
4. python3 -c "from kicad_agent.training.grpo_trainer import GRPOLoopTrainer; print('OK')" (grpo_trainer unchanged)
5. pytest tests/ -x --timeout=120 (no regressions)
</verification>

<success_criteria>
- AdvantageWeightedConfig replaces GRPOConfig as primary name
- AdvantageWeightedTrainer replaces GRPOTrainer as primary name
- kl_coefficient field removed from config (unused dead code)
- Backward-compatible aliases: GRPOConfig = AdvantageWeightedConfig, GRPOTrainer = AdvantageWeightedTrainer
- All existing consumer imports work via aliases without modification
- grpo_trainer.py completely untouched
- training/__init__.py docstring updated
- 8+ tests in test_grpo_rename.py pass
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/78-known-limitations-fixes/78-02-SUMMARY.md`
</output>
