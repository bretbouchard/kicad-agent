---
phase: 101-schematic-ops-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kicad_agent/ops/repair_components.py
  - tests/test_schematic_repair.py
autonomous: true
requirements: [R-1]
tags: [bug-fix, attribute-access, update-symbols]

must_haves:
  truths:
    - "update_symbols_from_library completes without AttributeError on schematics with lib_symbol_mismatch"
    - "The op correctly matches symbols by libId or entryName (not name)"
    - "Existing schematic_repair tests pass (zero regression)"
  artifacts:
    - path: "src/kicad_agent/ops/repair_components.py"
      provides: "Fixed symbol lookup using sym.entryName instead of sym.name"
      contains: "entryName"
    - path: "tests/test_schematic_repair.py"
      provides: "Regression test for update_symbols_from_library crash"
      contains: "test_update_symbols_from_library"
  key_links:
    - from: "src/kicad_agent/ops/repair_components.py:146"
      to: "kiutils Symbol class"
      via: "sym.entryName attribute access (not sym.name)"
      pattern: "entryName"
---

<objective>
Fix the `update_symbols_from_library` crash (P0-001) — a one-line attribute access bug where `sym.name` should be `sym.entryName`.

Per CONTEXT.md D-1 (locked decision): Fix all 5 bugs, no descoping.
Per CONTEXT.md D-3: General fix only, no backplane-specific workarounds.

Purpose: This bug blocks 242 ERC violations on the analog-ecosystem backplane and affects all multi-sheet KiCad 10 projects. The fix is a single attribute swap — kiutils `Symbol` class exposes `libId` (property) and `entryName` (field), NOT `name`.

Output: One-line fix in repair_components.py + regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/101-schematic-ops-bug-fixes/CONTEXT.md
@.planning/phases/101-schematic-ops-bug-fixes/101-RESEARCH.md

<interfaces>
<!-- Verified bug location and kiutils Symbol class API -->

From src/kicad_agent/ops/repair_components.py:144-148 [VERIFIED BUG]:
```python
source_symbol = None
for sym in lib.symbols:
    if sym.libId == lib_id or sym.name == symbol_name:  # ← AttributeError: 'Symbol' object has no attribute 'name'
        source_symbol = sym
        break
```

From /opt/homebrew/lib/python3.11/site-packages/kiutils/symbol.py [VERIFIED]:
- Line 221: `libId` is a @property (returns the library ID like "Device:R")
- Line 297: `entryName` is a field (returns the symbol name like "R")
- NO `name` attribute exists on the Symbol class

The fix: replace `sym.name` with `sym.entryName` (handles unqualified names like "R")
The `sym.libId == lib_id` clause already works (handles qualified IDs like "Device:R")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test for update_symbols_from_library crash, then fix sym.name → sym.entryName</name>
  <files>tests/test_schematic_repair.py, src/kicad_agent/ops/repair_components.py</files>
  <read_first>
    - src/kicad_agent/ops/repair_components.py (lines 120-160 — update_symbols_from_library function + bug site at line 146)
    - tests/test_schematic_repair.py (first 80 lines — understand test imports, fixture patterns, and how existing tests build SchematicIR)
    - tests/test_schematic_repair.py (find existing update_symbols tests via grep — extend or add new test)
  </read_first>
  <behavior>
    - Test 1: `test_update_symbols_from_library_no_crash_on_mismatch` — Build a minimal schematic with a lib_symbol_mismatch (or use existing fixture). Call `update_symbols_from_library(ir, file_path)`. Assert NO AttributeError is raised. The op may report "symbol_not_in_library" for missing libs (that is correct behavior), but must NOT crash with `'Symbol' object has no attribute 'name'`.
    - Test 2: `test_update_symbols_from_library_uses_entryName_for_matching` — Build a schematic where the symbol name does NOT match `libId` but DOES match `entryName`. Verify the op finds and re-embeds the symbol (source_symbol is not None).
  </behavior>
  <action>
**RED phase — write tests first:**

1. In `tests/test_schematic_repair.py`, add the 2 tests listed in `<behavior>`.
   - For Test 1: Use the minimal schematic fixture pattern from existing tests in the file. The test needs a SchematicIR with at least one symbol that has a lib_symbol_mismatch. If the existing test file has a helper to build such a fixture, reuse it. Otherwise, build a minimal .kicad_sch in a temp file with a symbol whose embedded lib_symbol graphics differ from the canonical library.
   - Key assertion: the function RETURNS a dict (does not raise). The dict structure is `{"updated": [...], "skipped": [...]}`. Check that no AttributeError leaks out.
   - For Test 2: Build a schematic where `sym.libId` is "Device:R" but the lookup is by symbol_name="R" (unqualified). After the fix, `sym.entryName == "R"` matches.

2. Run the tests — they MUST fail with `AttributeError: 'Symbol' object has no attribute 'name'` (RED confirmation).

**GREEN phase — apply the one-line fix:**

3. In `src/kicad_agent/ops/repair_components.py` line 146, change:
   ```python
   if sym.libId == lib_id or sym.name == symbol_name:
   ```
   to:
   ```python
   if sym.libId == lib_id or sym.entryName == symbol_name:
   ```

4. Add a comment above the line explaining WHY:
   ```python
   # kiutils Symbol class exposes libId (property) and entryName (field).
   # It does NOT have a `name` attribute — using sym.name raises AttributeError.
   # libId matches qualified IDs ("Device:R"); entryName matches unqualified ("R").
   # [P0-001 fix] See BUGS/P0-001-update-symbols-from-library-crash.md
   ```

5. Run the tests again — they MUST pass (GREEN confirmation).

IMPORTANT:
- This is a ONE-LINE fix. Do NOT refactor the surrounding code.
- Do NOT change the function signature, return type, or any other logic.
- Do NOT add a `name` property to any class — the fix is in the consumer, not the kiutils library.
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python3 -m pytest tests/test_schematic_repair.py -k "update_symbols" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - repair_components.py line ~146 contains `sym.entryName == symbol_name` (NOT `sym.name`)
    - repair_components.py does NOT contain `sym.name` anywhere in the update_symbols_from_library function (grep -n "sym\.name" should return 0 matches in that function)
    - tests/test_schematic_repair.py contains `test_update_symbols_from_library_no_crash_on_mismatch`
    - Both new tests pass
    - Existing tests in test_schematic_repair.py still pass (zero regression)
  </acceptance_criteria>
  <done>
    - `sym.name` → `sym.entryName` fix applied at repair_components.py:146
    - 2 regression tests pass
    - No AttributeError on update_symbols_from_library calls
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library file → op handler | Symbol library files parsed by kiutils; handler reads symbol attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-03 | T (Tampering) | Symbol library matching logic | mitigate | Fix ensures correct attribute access (entryName not name). No injection risk — string equality comparison only. |
| T-101-04 | D (Denial of Service) | Crash on valid input | mitigate | The AttributeError crash itself was the DoS. Fix eliminates it. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_schematic_repair.py -k update_symbols -x -q` — new tests pass
- `python3 -m pytest tests/test_schematic_repair.py -x -q` — full file, zero regression
- `grep -n "sym\.name" src/kicad_agent/ops/repair_components.py` — returns 0 matches in update_symbols_from_library function (the bug is gone)
</verification>

<success_criteria>
- SC-1 (R-1): `update_symbols_from_library` completes without AttributeError on backplane codecs.kicad_sch
- SC-7 (regression): Existing Phase 23 schematic repair tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/101-schematic-ops-bug-fixes/101-02-SUMMARY.md`
</output>
