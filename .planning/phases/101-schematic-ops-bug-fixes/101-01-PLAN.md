---
phase: 101-schematic-ops-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kicad_agent/ops/registry.py
  - src/kicad_agent/ops/erc_auto_fix.py
  - tests/test_erc_auto_fix.py
autonomous: true
requirements: [R-3]
tags: [deprecation, registry, erc-auto-fix]

must_haves:
  truths:
    - "OpMeta Pydantic model has a `deprecated: bool = False` field"
    - "OPERATION_REGISTRY entries for erc_auto_fix and erc_auto_fix_hierarchical both have deprecated=True"
    - "Calling erc_auto_fix or erc_auto_fix_hierarchical emits a DeprecationWarning"
    - "All existing erc_auto_fix tests still pass (zero regression)"
  artifacts:
    - path: "src/kicad_agent/ops/registry.py"
      provides: "deprecated field on OpMeta + deprecated=True on 2 ops"
      contains: "deprecated"
    - path: "src/kicad_agent/ops/erc_auto_fix.py"
      provides: "DeprecationWarning emission at handler entry"
      contains: "DeprecationWarning"
    - path: "tests/test_erc_auto_fix.py"
      provides: "deprecation warning + registry flag tests"
      contains: "test_erc_auto_fix_deprecation"
  key_links:
    - from: "src/kicad_agent/ops/erc_auto_fix.py"
      to: "src/kicad_agent/ops/registry.py"
      via: "OpMeta.deprecated field check at handler entry"
      pattern: "deprecated"
---

<objective>
Mark `erc_auto_fix` and `erc_auto_fix_hierarchical` as DEPRECATED to prevent ongoing data loss (P0-003).

Per CONTEXT.md D-2 (locked decision): This phase DEPRECATES both ops only. Full raw S-expr rewrite is explicitly deferred to a follow-up phase. The PWR_FLAG nesting bug is NOT fixed in-line.

Purpose: These ops corrupt KiCad 10 schematics via kiutils re-serialization (documented in `kiutils-root-sheet-danger.md`). Deprecation prevents accidental use while the raw S-expr rewrite is pending.

Output: `deprecated` field on OpMeta, `deprecated=True` on both registry entries, runtime DeprecationWarning at handler entry, regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-schematic-ops-bug-fixes/CONTEXT.md
@.planning/phases/101-schematic-ops-bug-fixes/101-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/kicad_agent/ops/registry.py:17-38:
```python
class OpMeta(BaseModel):
    op_type: str
    category: str
    description: str
    file_types: list[str]
    is_readonly: bool
    scope: Literal["single_point", "single_file", "multi_file"]
    requires: list[str]
    conflicts: list[str]
    # NEW: deprecated: bool = False  (added in this plan)
```

From src/kicad_agent/ops/registry.py:1144-1161:
```python
"erc_auto_fix": {
    "category": "erc_smart",
    "description": "Meta-operation: run ERC, dispatch repairs by violation type, iterate",
    "file_types": [".kicad_sch"],
    "is_readonly": False,
    "scope": "single_file",
    "requires": ["parse_erc"],
    "conflicts": [],
},
"erc_auto_fix_hierarchical": {
    "category": "erc_smart",
    "description": "Run ERC auto-fix across all sheets in a hierarchical schematic",
    ...
},
```

From src/kicad_agent/ops/erc_auto_fix.py (handler entry points):
- `erc_auto_fix()` function — per-sheet variant
- `erc_auto_fix_hierarchical()` function — hierarchical variant
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deprecated field to OpMeta + mark both erc_auto_fix ops</name>
  <files>src/kicad_agent/ops/registry.py, tests/test_erc_auto_fix.py</files>
  <read_first>
    - src/kicad_agent/ops/registry.py (lines 1-40 — OpMeta class definition)
    - src/kicad_agent/ops/registry.py (lines 1140-1162 — erc_auto_fix + erc_auto_fix_hierarchical registry entries)
    - tests/test_erc_auto_fix.py (first 60 lines — understand existing test structure and imports)
  </read_first>
  <behavior>
    - Test 1: `test_opmeta_has_deprecated_field` — OpMeta model has a `deprecated` field of type `bool` with default `False`
    - Test 2: `test_erc_auto_fix_registry_deprecated_flag` — `OPERATION_REGISTRY["erc_auto_fix"].deprecated is True`
    - Test 3: `test_erc_auto_fix_hierarchical_registry_deprecated_flag` — `OPERATION_REGISTRY["erc_auto_fix_hierarchical"].deprecated is True`
    - Test 4: `test_non_deprecated_ops_default_false` — a sampling of 5+ other ops (add_component, remove_component, etc.) have `deprecated is False`
  </behavior>
  <action>
1. Add `deprecated: bool = False` field to the `OpMeta` class in `src/kicad_agent/ops/registry.py` (after `conflicts` field, line ~38). Add docstring line: "deprecated: True if this op is deprecated and should not be used."

2. Add `"deprecated": True` to BOTH registry catalog entries in `_RAW_CATALOG`:
   - `"erc_auto_fix"` (line 1144) — append `"deprecated": True,` to the dict
   - `"erc_auto_fix_hierarchical"` (line 1153) — append `"deprecated": True,` to the dict

3. Verify the registry population code (search for where `_RAW_CATALOG` is consumed to build `OpMeta` instances — likely iterates dict keys). If it uses `OpMeta(**entry)` or `OpMeta(op_type=k, **v)`, the new field will be picked up automatically since Pydantic accepts the new key. If it filters keys explicitly, add `deprecated` to the allowed list.

4. Write the 4 tests listed in `<behavior>` in `tests/test_erc_auto_fix.py`. Import `OPERATION_REGISTRY` and `OpMeta` from `kicad_agent.ops.registry`.

IMPORTANT: Do NOT modify the handler code (erc_auto_fix.py) in this task — that is Task 2.
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python3 -m pytest tests/test_erc_auto_fix.py -k "deprecated or opmeta" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - registry.py OpMeta class contains `deprecated: bool = False`
    - registry.py _RAW_CATALOG["erc_auto_fix"] contains `"deprecated": True`
    - registry.py _RAW_CATALOG["erc_auto_fix_hierarchical"] contains `"deprecated": True`
    - tests/test_erc_auto_fix.py contains `test_erc_auto_fix_registry_deprecated_flag`
    - tests/test_erc_auto_fix.py contains `test_erc_auto_fix_hierarchical_registry_deprecated_flag`
    - All 4 new tests pass
  </acceptance_criteria>
  <done>
    - OpMeta has `deprecated` field with default False
    - Both erc_auto_fix ops have deprecated=True in registry
    - 4 new tests pass, existing tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit DeprecationWarning at erc_auto_fix handler entry points</name>
  <files>src/kicad_agent/ops/erc_auto_fix.py, tests/test_erc_auto_fix.py</files>
  <read_first>
    - src/kicad_agent/ops/erc_auto_fix.py (first 60 lines — imports + module structure)
    - src/kicad_agent/ops/erc_auto_fix.py (lines 170-200 — erc_auto_fix function entry)
    - src/kicad_agent/ops/erc_auto_fix.py (lines 630-660 — erc_auto_fix_hierarchical function entry)
  </read_first>
  <behavior>
    - Test 5: `test_erc_auto_fix_emits_deprecation_warning` — calling `erc_auto_fix(ir, file_path)` raises DeprecationWarning via `pytest.warns(DeprecationWarning)`. The warning message contains "DEPRECATED" and references "P0-003" or "raw S-expr".
    - Test 6: `test_erc_auto_fix_hierarchical_emits_deprecation_warning` — calling `erc_auto_fix_hierarchical` raises DeprecationWarning.
  </behavior>
  <action>
1. At the TOP of the `erc_auto_fix()` function body (first line after the docstring, before any other logic), add:
```python
import warnings
warnings.warn(
    "erc_auto_fix is DEPRECATED (P0-003): kiutils re-serialization corrupts "
    "KiCad 10 schematics. Use targeted individual ops (add_no_connect, "
    "place_no_connects_from_erc, add_power_flags, remove_dangling_wires) "
    "instead. Full raw S-expr rewrite tracked as follow-up. See BUGS/P0-003.md.",
    DeprecationWarning,
    stacklevel=2,
)
```

2. At the TOP of the `erc_auto_fix_hierarchical()` function body (first line after docstring), add the same warning block (adjust the op name in the message to `erc_auto_fix_hierarchical`).

3. Write tests 5 and 6 in `tests/test_erc_auto_fix.py`. Use `pytest.warns(DeprecationWarning)` context manager. The tests should build a minimal SchematicIR + temp .kicad_sch file (or reuse existing fixtures from the test file — check what other tests use for setup) and call the handler. The warning must fire BEFORE any file mutation, so the test does not need to verify side effects.

IMPORTANT:
- Place the `import warnings` at module top-level if not already present (check existing imports first — do NOT add duplicate imports).
- Use `stacklevel=2` so the warning points to the CALLER, not the handler.
- Do NOT change any other logic in these functions. Do NOT touch the `to_file()` call sites. Do NOT add raw S-expr parsing. This is deprecation ONLY (per CONTEXT.md D-2).
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python3 -m pytest tests/test_erc_auto_fix.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - erc_auto_fix.py contains `warnings.warn` with `DeprecationWarning` in erc_auto_fix function
    - erc_auto_fix.py contains `warnings.warn` with `DeprecationWarning` in erc_auto_fix_hierarchical function
    - Warning message contains "DEPRECATED"
    - Warning message contains "P0-003"
    - tests/test_erc_auto_fix.py contains `test_erc_auto_fix_emits_deprecation_warning`
    - tests/test_erc_auto_fix.py contains `test_erc_auto_fix_hierarchical_emits_deprecation_warning`
    - Full test_erc_auto_fix.py suite passes (zero regression on existing tests)
  </acceptance_criteria>
  <done>
    - Both handler entry points emit DeprecationWarning
    - Warning fires before any file mutation
    - All existing erc_auto_fix tests still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → op handler | User calls deprecated op; warning must surface before any file mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | T (Tampering) | erc_auto_fix file serialization | accept | kiutils re-serialization corruption is the known bug being deprecated. No mitigation in this plan — the op is marked deprecated to prevent use. Full fix deferred per CONTEXT.md D-2. |
| T-101-02 | I (Information Disclosure) | Deprecation warning message | accept | Warning message references BUGS/P0-003.md for context. No sensitive data in message. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_erc_auto_fix.py -x -q` — all tests pass (existing + 6 new)
- `python3 -m pytest tests/test_schematic_repair.py tests/test_place_no_connects_power_aware.py -x -q` — zero regression on sibling test files
- `grep -c "deprecated.*True" src/kicad_agent/ops/registry.py` returns 2 (both erc_auto_fix entries)
</verification>

<success_criteria>
- SC-4 (R-3): Both `erc_auto_fix` and `erc_auto_fix_hierarchical` are marked DEPRECATED in op metadata
- SC-7 (regression): Existing Phase 40 ERC tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/101-schematic-ops-bug-fixes/101-01-SUMMARY.md`
</output>
