---
phase: 97
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/vast_launch_kicad.sh
  - scripts/vast_train_kicad.py
  - tests/test_vast_train_kicad.py
autonomous: true
requirements: [D-06, D-07, D-08, D-09, D-10, D-11, D-12]

must_haves:
  truths:
    - "vast_launch_kicad.sh searches for RTX 3090 instances, creates instance, uploads training script, launches training"
    - "vast_train_kicad.py loads Gemma 4 12B in 4-bit quantization, applies LoRA rank 16, trains with SFTTrainer"
    - "Training script preserves all proven classes from spectral-primitives (HeartbeatCallback, Gemma4VisionCollator, dequantize_vision_encoder)"
    - "No hardcoded credentials or API keys in either script"
  artifacts:
    - path: "scripts/vast_launch_kicad.sh"
      provides: "Vast.ai instance launch and training orchestration script"
      min_lines: 180
      contains: "set -euo pipefail"
    - path: "scripts/vast_train_kicad.py"
      provides: "CUDA LoRA training script for Gemma 4 12B on Vast.ai"
      min_lines: 300
      contains: "def main"
    - path: "tests/test_vast_train_kicad.py"
      provides: "Config validation tests for training script"
      min_lines: 50
      contains: "class TestVastTrainKicad"
  key_links:
    - from: "scripts/vast_launch_kicad.sh"
      to: "scripts/vast_train_kicad.py"
      via: "SCP upload + nohup python execution"
      pattern: "vast_train_kicad"
    - from: "scripts/vast_train_kicad.py"
      to: "google/gemma-4-12b-it"
      via: "AutoModelForMultimodalLM.from_pretrained"
      pattern: "gemma-4-12b"
---

<objective>
Adapt the proven spectral-primitives Vast.ai training scripts for KiCad vision LoRA training. Copy vast_launch.sh and vast_train_gemma4.py from spectral-primitives, change only the configuration values (dataset reference, output paths, script names, instance labels), and keep all proven code (HeartbeatCallback, Gemma4VisionCollator, dequantize_vision_encoder) unchanged.

Purpose: This provides the remote GPU training infrastructure. The spectral-primitives scripts are battle-tested and successfully trained a Gemma 4 vision LoRA. Adapting them minimizes risk — we change config, not code.

Output: `vast_launch_kicad.sh` and `vast_train_kicad.py` ready for remote training execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-kicad-vision-lora-training-execution/97-CONTEXT.md
@.planning/phases/97-kicad-vision-lora-training-execution/97-RESEARCH.md
@.planning/phases/97-kicad-vision-lora-training-execution/97-PATTERNS.md

<interfaces>
<!-- Source templates to copy and adapt. The executor MUST read these in full. -->

Template 1: ~/apps/spectral-primitives/scripts/vast_launch.sh (225 lines)
- Configuration section (lines 17-23): GPU_NAME, MAX_PRICE, IMAGE, DATASET_KAGGLE, MAX_STEPS, OUTPUT_DIR
- Disk allocation (line 79): --disk 40
- Instance label (line 82): --label
- Script upload (line 135): vastai scp
- Training launch (line 203): nohup python
- Adapter download (line 222): vastai scp -r

Template 2: ~/apps/spectral-primitives/scripts/vast_train_gemma4.py (361 lines)
- Argparse defaults (lines 175-195): --dataset_path, --output_dir, --model_id, --lora_rank, etc.
- HeartbeatCallback class (lines 37-76): NO CHANGES
- dequantize_vision_encoder function (lines 79-100): NO CHANGES
- Gemma4VisionCollator class (lines 103-154): NO CHANGES
- find_latest_checkpoint function (lines 157-170): NO CHANGES
- Main training flow (lines 173-361): Only arg defaults change
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vast_train_kicad.py by copying and adapting vast_train_gemma4.py</name>
  <files>scripts/vast_train_kicad.py</files>
  <read_first>
    - /Users/bretbouchard/apps/spectral-primitives/scripts/vast_train_gemma4.py (FULL 361 lines — the template to copy)
    - .planning/phases/97-kicad-vision-lora-training-execution/97-RESEARCH.md (lines 677-693 — adaptation table)
  </read_first>
  <action>
Copy `/Users/bretbouchard/apps/spectral-primitives/scripts/vast_train_gemma4.py` to `scripts/vast_train_kicad.py`. Then make ONLY these changes (per D-08, D-09, D-10, D-11, D-12):

1. **Docstring (line 2-3):** Change description to:
```python
"""Standalone Gemma 4 12B LoRA training script for KiCad vision (Vast.ai / CUDA).

Trains on unified KiCad vision dataset (maze spatial reasoning + PCB analysis).
Adapted from spectral-primitives vast_train_gemma4.py.
"""
```

2. **Argparse `--output_dir` default (line 177):** Change from `/workspace/gemma4-lora-adapter` to `/workspace/kicad-vision-lora-adapter`

3. **Argparse `--max_seq_length` default (line 189):** Change from `2048` to `4096` (exploration chains can be 3000+ chars per RESEARCH.md Open Question 3)

4. **Argparse `--dataset_path` help text (line 176):** Change to `"Path to unified KiCad vision dataset (HuggingFace save_to_disk format)"`

5. **Argparse description (line 174):** Change to `"Gemma 4 LoRA training for KiCad vision (Vast.ai / CUDA)"`

6. **Keep UNCHANGED (do NOT modify these):**
   - All imports (lines 14-30)
   - HeartbeatCallback class (lines 37-76)
   - dequantize_vision_encoder function (lines 79-100)
   - Gemma4VisionCollator class (lines 103-154)
   - find_latest_checkpoint function (lines 157-170)
   - LoRA config: rank=16, alpha=32, target_modules=["q_proj", "k_proj", "v_proj", "o_proj"] (D-10)
   - BitsAndBytesConfig: 4-bit NF4 quantization (D-10)
   - SFTConfig: all proven settings
   - Main training flow logic (lines 197-361)
   - `--model_id` default: keep `google/gemma-4-12b-it` (D-09)
   - `--max_steps` default: keep 400 (D-11)

The resulting file should be a near-exact copy with only the 5 changes listed above.
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python -c "import ast; ast.parse(open('scripts/vast_train_kicad.py').read()); print('syntax ok')" && grep -c "class HeartbeatCallback" scripts/vast_train_kicad.py && grep -c "class Gemma4VisionCollator" scripts/vast_train_kicad.py && grep -c "dequantize_vision_encoder" scripts/vast_train_kicad.py</automated>
  </verify>
  <acceptance_criteria>
    - scripts/vast_train_kicad.py contains `class HeartbeatCallback`
    - scripts/vast_train_kicad.py contains `class Gemma4VisionCollator`
    - scripts/vast_train_kicad.py contains `def dequantize_vision_encoder`
    - scripts/vast_train_kicad.py contains `kicad-vision-lora-adapter` (changed output_dir)
    - scripts/vast_train_kicad.py contains `4096` (changed max_seq_length)
    - scripts/vast_train_kicad.py contains `google/gemma-4-12b-it` (unchanged model_id)
    - scripts/vast_train_kicad.py contains `LoraConfig` with `r=16` or `r: 16` or rank 16
    - scripts/vast_train_kicad.py contains `BitsAndBytesConfig`
    - scripts/vast_train_kicad.py does NOT contain `spectral` (except in docstring attribution)
    - `python -c "import ast; ast.parse(open('scripts/vast_train_kicad.py').read())"` exits 0
  </acceptance_criteria>
  <done>
    vast_train_kicad.py is a valid Python file with all spectral-primitives classes preserved, only output_dir and max_seq_length defaults changed, model_id unchanged at gemma-4-12b-it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create vast_launch_kicad.sh by copying and adapting vast_launch.sh</name>
  <files>scripts/vast_launch_kicad.sh</files>
  <read_first>
    - /Users/bretbouchard/apps/spectral-primitives/scripts/vast_launch.sh (FULL 225 lines — the template to copy)
    - .planning/phases/97-kicad-vision-lora-training-execution/97-RESEARCH.md (lines 694-710 — adaptation table)
    - .planning/phases/97-kicad-vision-lora-training-execution/97-PATTERNS.md (lines 208-258 — configuration changes)
  </read_first>
  <action>
Copy `/Users/bretbouchard/apps/spectral-primitives/scripts/vast_launch.sh` to `scripts/vast_launch_kicad.sh`. Then make ONLY these changes (per D-06, D-07):

1. **Header comment (lines 2-3):** Change to:
```bash
# Vast.ai Gemma 4 LoRA Training — KiCad Vision Launch Script
# Adapted from spectral-primitives vast_launch.sh
```

2. **Configuration section (lines 17-23):** Change these values:
```bash
GPU_NAME="RTX_3090"          # Keep (same GPU, D-10)
MAX_PRICE="0.20"             # Keep
IMAGE="pytorch/pytorch:2.5.1-cuda12.4-cudnn9-devel"  # Keep
DATASET_KAGGLE=""            # Change: empty (use SCP upload instead of Kaggle)
MAX_STEPS=400                # Keep (D-11)
OUTPUT_DIR="/workspace/kicad-vision-lora-adapter"  # Change from gemma4-lora-adapter
```

3. **Echo header (line 30):** Change to `=== Vast.ai KiCad Vision LoRA Training Setup ===`

4. **Disk allocation (line 79):** Change `--disk 40` to `--disk 50` (larger dataset needs more space per RESEARCH.md Pitfall 4)

5. **Instance label (line 82):** Change `--label "gemma4-lora-train"` to `--label "kicad-vision-lora-train"`

6. **Script upload (line 135):** Change `scripts/vast_train_gemma4.py` to `scripts/vast_train_kicad.py` and the destination to `vast_train_kicad.py`

7. **Dataset transfer (lines 140-169):** Replace the Kaggle download section with SCP upload:
```bash
echo -e "${GREEN}Uploading KiCad vision dataset via SCP...${NC}"
vastai scp -r training_output/unified_vision_data/ "${INSTANCE_ID}:/workspace/unified_vision_data/"
```

8. **Training launch (line 203):** Change `python vast_train_gemma4.py` to `python vast_train_kicad.py` and update dataset path to `--dataset_path /workspace/unified_vision_data/train`

9. **Adapter download path (line 222):** Change `./output/gemma4_adapter_vast/` to `./output/kicad_vision_adapter_vast/`

10. **pip install section:** Add matplotlib and pillow to the install line (needed if dataset needs re-processing on instance, though usually pre-processed):
```bash
pip install git+https://github.com/huggingface/transformers.git \
    peft>=0.15.0 bitsandbytes>=0.45.0 accelerate>=1.3.0 \
    trl==1.5.1 datasets>=3.0.0 sentencepiece protobuf
```
(transformers, peft, bitsandbytes already installed; matplotlib/pillow only needed for rendering, not training)

11. **Keep UNCHANGED:**
    - `set -euo pipefail` (line 15)
    - Color definitions (lines 26-28)
    - Prerequisites check (vastai CLI, HF_TOKEN) (lines 32-40)
    - Instance search logic
    - Instance creation logic
    - Wait-for-ready logic
    - SSH connection logic
    - Cost estimate structure

Make the script executable: `chmod +x scripts/vast_launch_kicad.sh`
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && bash -n scripts/vast_launch_kicad.sh && echo "syntax ok" && grep -c "set -euo pipefail" scripts/vast_launch_kicad.sh && grep -c "vast_train_kicad" scripts/vast_launch_kicad.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/vast_launch_kicad.sh contains `set -euo pipefail`
    - scripts/vast_launch_kicad.sh contains `kicad-vision-lora-adapter`
    - scripts/vast_launch_kicad.sh contains `kicad-vision-lora-train`
    - scripts/vast_launch_kicad.sh contains `vast_train_kicad.py`
    - scripts/vast_launch_kicad.sh contains `--disk 50`
    - scripts/vast_launch_kicad.sh contains `unified_vision_data`
    - scripts/vast_launch_kicad.sh does NOT contain hardcoded API_KEY, SECRET, PASSWORD, or TOKEN values
    - `bash -n scripts/vast_launch_kicad.sh` exits 0 (syntax valid)
    - `grep -r "API_KEY\|SECRET\|PASSWORD" scripts/vast_launch_kicad.sh` returns nothing (no hardcoded secrets)
  </acceptance_criteria>
  <done>
    vast_launch_kicad.sh is a valid bash script with all spectral-primitives logic preserved, config values changed to KiCad-specific paths/labels, dataset transfer via SCP, no hardcoded credentials.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create config validation tests for vast_train_kicad.py</name>
  <files>tests/test_vast_train_kicad.py</files>
  <read_first>
    - scripts/vast_train_kicad.py (just created in Task 1 — validate its configuration)
    - tests/test_workflow_runner.py (FULL — pytest class structure pattern)
  </read_first>
  <behavior>
    - Test 1: vast_train_kicad.py is syntactically valid Python (ast.parse succeeds)
    - Test 2: Script contains HeartbeatCallback class (proven code preserved)
    - Test 3: Script contains Gemma4VisionCollator class (proven code preserved)
    - Test 4: Script contains dequantize_vision_encoder function (proven code preserved)
    - Test 5: Script default output_dir is kicad-vision-lora-adapter (not gemma4-lora-adapter)
    - Test 6: Script default max_seq_length is 4096 (not 2048)
    - Test 7: Script default model_id is google/gemma-4-12b-it (unchanged from spectral-primitives)
    - Test 8: LoRA rank default is 16 (D-10)
    - Test 9: vast_launch_kicad.sh contains set -euo pipefail (bash safety)
    - Test 10: vast_launch_kicad.sh does NOT contain hardcoded credentials
  </behavior>
  <action>
Create `tests/test_vast_train_kicad.py` with test class `TestVastTrainKicad`. Tests validate that the adapted scripts preserve all proven classes from spectral-primitives and have correct KiCad-specific config values.

Use AST parsing and grep-based assertions rather than importing the script (the script imports torch, transformers, etc. which may not be available in test environment).

Pattern:
```python
import ast
from pathlib import Path

SCRIPT_PATH = Path(__file__).resolve().parent.parent / "scripts" / "vast_train_kicad.py"
LAUNCH_PATH = Path(__file__).resolve().parent.parent / "scripts" / "vast_launch_kicad.sh"


class TestVastTrainKicad:
    """Validate vast_train_kicad.py preserves spectral-primitives proven code."""

    def test_syntax_valid(self):
        source = SCRIPT_PATH.read_text()
        ast.parse(source)  # Raises SyntaxError if invalid

    def test_has_heartbeat_callback(self):
        source = SCRIPT_PATH.read_text()
        assert "class HeartbeatCallback" in source

    # ... etc for all 10 behavior tests
```

Write tests FIRST (RED), then verify after Task 1 and 2 are complete (GREEN).
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python -m pytest tests/test_vast_train_kicad.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_vast_train_kicad.py contains `class TestVastTrainKicad`
    - tests/test_vast_train_kicad.py contains test methods for all 10 behavior tests
    - tests/test_vast_train_kicad.py uses ast.parse for syntax validation
    - tests/test_vast_train_kicad.py validates no hardcoded secrets in launch script
    - `python -m pytest tests/test_vast_train_kicad.py -x -q` exits 0
  </acceptance_criteria>
  <done>
    All 10 config validation tests pass, confirming spectral-primitives classes preserved and KiCad config values correct.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local machine → Vast.ai cloud | Training script and dataset uploaded to remote instance |
| Vast.ai instance → HuggingFace | Model download from HuggingFace gated repo |
| Vast.ai → Local machine | Trained adapter downloaded back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02 | Information Disclosure | Hardcoded credentials in launch script | mitigate | Use environment variables for HF_TOKEN; never hardcode API keys; test grep confirms no secrets in script |
| T-97-04 | Spoofing | Vast.ai instance impersonation | accept | vastai CLI handles SSH key management; instance ID verification built into CLI |
| T-97-05 | Tampering | Dataset integrity during SCP transfer | accept | HuggingFace dataset format includes checksums; training would fail on corrupted data |
</threat_model>

<verification>
- `python -m pytest tests/test_vast_train_kicad.py -x -q` passes
- `bash -n scripts/vast_launch_kicad.sh` syntax valid
- Both scripts exist with correct config values
- No hardcoded credentials found
</verification>

<success_criteria>
- vast_train_kicad.py is a valid copy of vast_train_gemma4.py with only output_dir, max_seq_length, and docstring changed
- vast_launch_kicad.sh is a valid copy of vast_launch.sh with KiCad-specific config values
- All proven classes (HeartbeatCallback, Gemma4VisionCollator, dequantize_vision_encoder) preserved unchanged
- Config validation tests confirm all adaptation points
</success_criteria>

<output>
After completion, create `.planning/phases/97-kicad-vision-lora-training-execution/97-02-SUMMARY.md`
</output>
