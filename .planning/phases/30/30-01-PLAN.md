# Phase 30 Plan: MCP Operations Server

**Phase:** 30
**Status:** COMPLETE
**Requirements:** MCPSRV-01 through MCPSRV-07, META-01 through META-03, PKG-01, PKG-02

## Summary

New MCP server binary (`kicad-agent-edit`) exposing all 57 kicad-agent operations as individually named MCP tools, plus 2 meta-tools for schema discovery and project context.

## What Was Done

All 3 plans merged into a single implementation pass:

### 30-01: Server Skeleton
- [x] Created `src/kicad_agent/mcp/edit_server.py` (~260 lines)
- [x] Dynamic tool generation from 57 `Operation` union variants via `model_json_schema()`
- [x] `Server("kicad-agent-edit")` with stdio transport and `server_lifespan`
- [x] `KICAD_PROJECT_DIR` env var for base directory (default `Path.cwd()`)
- [x] Added `kicad-agent-edit` entry point to `pyproject.toml`

### 30-02: Call Dispatcher + Error Handling
- [x] `dispatch_tool()` function for testability (separate from MCP ContextVar handler)
- [x] Pydantic validation via `Operation.model_validate({"root": payload})`
- [x] `asyncio.to_thread(executor.execute, op)` wrapping
- [x] `CallToolResult(isError=True)` for all error paths
- [x] 50KB response capping with truncation notice
- [x] ToolAnnotations: readOnlyHint (11 ops), destructiveHint (8 ops), idempotentHint (9 ops)
- [x] Error classification: validation_error, FileNotFoundError, PermissionError, generic with correlation ID

### 30-03: Meta-tools + Tests
- [x] `get_operation_schema` meta-tool (returns full Operation JSON Schema)
- [x] `get_project_context` meta-tool (wraps `render_project_context()`)
- [x] 32 tests in `tests/test_mcp/test_edit_server.py`

## Test Results

- MCP tests: 32 passed
- Full suite: 1673 passed, 0 failed, 0 regressions

## Files Changed

| File | Action | Lines |
|------|--------|-------|
| `src/kicad_agent/mcp/edit_server.py` | NEW | ~260 |
| `pyproject.toml` | MODIFIED | +1 (entry point) |
| `tests/test_mcp/test_edit_server.py` | NEW | ~300 |

## Verification

- [x] 59 tools registered (57 ops + 2 meta)
- [x] Each tool has correct inputSchema from Pydantic
- [x] ToolAnnotations correctly assigned per category
- [x] dispatch_tool routes to executor correctly
- [x] Errors return isError=True with structured JSON
- [x] Response capping works for >50KB outputs
- [x] No regressions in existing 1673 tests
