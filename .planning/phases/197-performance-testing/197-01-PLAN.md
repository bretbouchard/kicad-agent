---
phase: 197-performance-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Tests/PerformanceTests/, KiCadAgentApp/Daemon/, KiCadAgentApp/Memory/]
autonomous: true
requirements: [TEST-09, TEST-12]
user_setup: []

must_haves:
  truths:
    - "Performance tests measure latency (daemon spawn, op execution, pipeline steps)"
    - "Memory tests detect leaks (daemon memory stable over 10-hour run)"
    - "Regression tests detect slowdowns (latency regressions block merge)"
    - "Decision Timeline loads < 2 seconds with 100K events (Pitfall 8 prevention)"
    - "MLX-Swift VRAM detected at startup, 4B model loads without swap on 8GB devices (Pitfall 7 prevention)"
  artifacts:
    - path: "Tests/PerformanceTests/"
      provides: "Performance test suite with baseline metrics"
      min_lines: 80
    - path: "KiCadAgentApp/Daemon/DaemonMCPClient.swift"
      provides: "Instrumented daemon with latency metrics"
      exports: ["spawnLatency", "opExecutionTime"]
  key_links:
    - from: "Tests/PerformanceTests/"
      to: "KiCadAgentApp/Daemon/"
      via: "Performance tests measure spawn time, op latency"
      pattern: "measure\\(Block\\) \\{.*Process\\.run"
    - from: "Tests/PerformanceTests/"
      to: "KiCadAgentApp/Memory/"
      via: "Memory leak tests monitor SwiftData query performance"
      pattern: "XCTMemoryTest.*evaluate.*DecisionTimeline"

---

<objective>
Build performance test suite measuring latency (daemon spawn, op execution, pipeline steps), memory stability (daemon leaks over 10-hour run), regression detection (slowdowns block merge), Decision Timeline load time <2s with 100K events, and MLX-Swift VRAM detection.

Purpose: Performance regressions are silent killers. Daemon spawn latency increases from 100ms to 5s gradually. Decision Timeline slows from 0.5s to 10s as events accumulate. MLX-Swift loads 4B model on 8GB Mac and swaps to disk, killing app responsiveness. Performance tests establish baselines and fail CI when regressions detected.

Output: Performance test suite with XCTestMetrics baseline, memory leak tests (10-hour daemon run), Decision Timeline load test with 100K event fixture, MLX-Swift VRAM detection test, CI gate that blocks merge on latency regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/v6/STACK.md
@.planning/research/v6/ARCHITECTURE.md
@.planning/research/v6/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create performance test infrastructure and baselines</name>
  <files>Tests/PerformanceTests/PerformanceTests.swift, Tests/PerformanceTests/MemoryLeakTests.swift</files>
  <behavior>
    - Test 1: Daemon spawn completes within 200ms (baseline measured)
    - Test 2: Single op execution completes within 100ms (baseline)
    - Test 3: Decision Timeline loads 1K events in <500ms (baseline)
    - Test 4: Memory baseline established for daemon process
  </behavior>
  <action>Create PerformanceTests.swift with measure() blocks for daemon spawn, op execution, Decision Timeline load. Add MemoryLeakTests.swift with XCTMemoryTest for daemon long-run. Use Process() to measure spawn time: let start = Date(); try process.run(); let duration = Date().timeIntervalSince(start). Establish baselines: spawn<200ms, op<100ms, timeline<500ms. Write baselines to PERFORMANCE_BASELINES.md.</action>
  <verify>swift test --filter PerformanceTests && cat Tests/PerformanceTests/PERFORMANCE_BASELINES.md</verify>
  <done>Performance test infrastructure created with documented baselines for spawn, op, timeline load</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add latency instrumentation to DaemonMCPClient</name>
  <files>KiCadAgentApp/Daemon/DaemonMCPClient.swift</files>
  <behavior>
    - Test 1: spawn() method emits spawnLatency metric
    - Test 2: callTool() method emits opExecutionTime metric
    - Test 3: Metrics logged to OpJournal (performance audit trail)
    - Test 4: Metrics expose via XCTestMetrics for baseline comparison
  </behavior>
  <action>Add latency tracking to DaemonMCPClient. In spawn(): wrap process.run() with measure { let start = CFAbsoluteTimeGetCurrent(); try process.run(); let duration = CFAbsoluteTimeGetCurrent() - start; emit metric }. In callTool(): measure tool call duration. Log metrics to OpJournal (add executionTimeMs field). Expose via os_signpost for Instruments profiling. Create DaemonPerformanceTests that assert metrics are recorded.</action>
  <verify>swift test --filter DaemonPerformanceTests && instruments -s "Daemon Spawn" trace profile exists</verify>
  <done>Daemon latency instrumented with metrics logged to journal and exposed via XCTestMetrics</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build 10-hour memory leak test for daemon</name>
  <files>Tests/PerformanceTests/MemoryLeakTests.swift, Tests/fixtures/100k_events.jsonl</files>
  <behavior>
    - Test 1: Daemon memory stable over 1 hour (no growth >10MB)
    - Test 2: SwiftData query memory stable (no accumulation)
    - Test 3: MCP server doesn't leak file handles (lsof count stable)
    - Test 4: 10-hour extrapolation from 1-hour sample (linear growth acceptable, exponential not)
  </behavior>
  <action>Write MemoryLeakTests.swift with XCTMemoryTest.Heap test for daemon long-run. Spawn daemon, load 100K event fixture (generate via script: python3 -c "for i in range(100000): print(json.dumps({'event_id': str(uuid.uuid4()), 'timestamp': datetime.now().isoformat()}))"), run for 1 hour, measure memory growth. Use measure(metrics: [.wallClockTime, .memoryPhysical]) block. Assert memory growth <10MB/hour. Test file handle leaks: run lsof -p <daemon_pid> before/after, count stable.</action>
  <verify>swift test --filter MemoryLeakTests --run-for 1h</verify>
  <done>10-hour memory leak test created with <10MB/hour growth threshold, fixture generation script added</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create Decision Timeline load test with 100K event fixture</name>
  <files>Tests/PerformanceTests/DecisionTimelinePerformanceTests.swift, Tests/fixtures/100k_decision_events.jsonl</files>
  <behavior>
    - Test 1: Decision Timeline loads in <2 seconds with 100K events (Pitfall 8 prevention)
    - Test 2: Pagination loads chunks (not entire timeline at once)
    - Test 3: Query uses index on timestamp (verify via EXPLAIN QUERY PLAN)
    - Test 4: UI doesn't freeze during load (async rendering verified)
  </behavior>
  <action>Create DecisionTimelinePerformanceTests.swift. Generate 100K decision event fixture (decisions with conversationId, timestamp, eventData). In test: load fixture into SwiftData, start timer, call DecisionTimelineView.loadTimeline(), assert duration <2.0s. Verify pagination: timeline loads first 100 events only, lazy-loads more on scroll. Check SwiftData query uses index: fetch descriptor has predicate on timestamp, verified by logging query plan. Run on 8GB M1 MacBook Air to stress test.</action>
  <verify>swift test --filter DecisionTimelinePerformanceTests && sqlite3 kicadagent.db "EXPLAIN QUERY PLAN SELECT * FROM DecisionEvent WHERE timestamp > ?"</verify>
  <done>Decision Timeline loads 100K events in <2s with pagination, query uses timestamp index</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create MLX-Swift VRAM detection and 4B model load test</name>
  <files>Tests/PerformanceTests/MLXPerformanceTests.swift, KiCadAgentApp/Providers/MLXSwiftProvider.swift</files>
  <behavior>
    - Test 1: VRAM detected at startup via Metal.device.availableMemory
    - Test 2: 4B model refuses to load if VRAM <3GB (Pitfall 7 prevention)
    - Test 3: 4B model loads without swap on 8GB devices (measured via Instruments)
    - Test 4: VRAM warning shown to user if device capacity insufficient
  </behavior>
  <action>Add VRAM detection to MLXSwiftProvider: let device = MTLCreateSystemDefaultDevice(); let vramGB = device.currentAllocatedSize / 1_000_000_000. Refuse load if vramGB < 3.0 (4B model requires ~2.5GB VRAM + app overhead). Write MLXPerformanceTests.swift: testVRAMDetection() verifies device.vram > 0, test4BModelLoadRefusesOnLowVRAM() asserts load throws on <3GB device, test4BModelLoadsWithoutSwapOn8GB() measures memory pressure during load (verify no swap via vm.stat data). Test on real 8GB M1 MacBook Air.</action>
  <verify>swift test --filter MLXPerformanceTests && xcrun simctl launch --console && log show | grep -i "metal.*memory"</verify>
  <done>MLX-Swift VRAM detection implemented, 4B model refuses load on low VRAM, loads without swap on 8GB devices</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Create regression detection CI gate</name>
  <files>.github/workflows/performance-regression.yml, Tests/PerformanceTests/BaselineMetrics.json</files>
  <behavior>
    - Test 1: CI fails if daemon spawn >2x baseline (400ms)
    - Test 2: CI fails if op execution >2x baseline (200ms)
    - Test 3: CI fails if Decision Timeline >2x baseline (4s)
    - Test 4: CI fails if memory growth >2x baseline (20MB/hour)
  </behavior>
  <action>Create performance-regression.yml CI workflow. Run performance tests on every PR. Load baseline metrics from BaselineMetrics.json. Compare current metrics against baseline with 2x threshold. Fail workflow if any metric exceeds threshold. Update baseline manually on approved performance improvements. Add job to .github/workflows/ with macos-latest runner, Xcode 16+. Workflow uploads BaselineMetrics.json as artifact for comparison.</action>
  <verify>cat .github/workflows/performance-regression.yml && gh workflow view performance-regression</verify>
  <done>CI regression gate created, fails PR on >2x latency/memory growth threshold, baseline tracked in json</done>
</task>

<task type="auto">
  <name>Task 5: Create nightly stress workflow (TEST-12)</name>
  <files>.github/workflows/nightly-stress.yml</files>
  <action>Create nightly-stress.yml CI workflow for stress tests:
1. **Schedule**: Cron trigger for daily 2 AM UTC (off-peak hours)
   ```yaml
   on:
     schedule:
       - cron: '0 2 * * *'  # Daily 2 AM UTC
     workflow_dispatch:  # Allow manual trigger
   ```

2. **Job 1: 10-hour daemon memory leak test** (per TEST-12):
   - Launch KiCadAgent app with daemon
   - Run 100 random KiCad operations every 30 minutes
   - Monitor daemon memory via `ps -o rss= -p <daemon_pid>`
   - Log memory every 5 minutes to stress-memory.log
   - Fail if memory growth >100MB/hour (leak threshold)
   - Timeout after 10 hours (kill daemon, upload logs)

3. **Job 2: Multi-account CloudKit simulation** (per TEST-12):
   - Create test fixture with 3 simulated iCloud accounts (account1, account2, account3)
   - Use CKShare test data: Create project in account1, share with account2 and account3
   - Simulate concurrent edits: All 3 accounts modify conversation simultaneously
   - Verify CloudKit merge resolves conflicts correctly (no data loss)
   - Test share acceptance/decline flow for each account
   - Fail if any account sees inconsistent conversation state

4. **Job 3: Multi-session Group Activities test** (per TEST-12 and Pitfall 10):
   - **Hardware requirement**: 2+ physical Macs with FaceTime/iMessage signed in
   - Create Group Activity session between Mac1 and Mac2
   - Both Macs join same live KiCad design session
   - Simultaneous edits: Mac1 adds component, Mac2 routes wire
   - Verify CKShare participant permissions work correctly (both see updates)
   - Test session leave/rejoin (one Mac exits, other continues)
   - Fail if Group Activities state diverges between sessions
   - **Note**: If only 1 physical Mac available, skip Job 3 with warning "Group Activities requires 2+ physical devices per PITFALLS.md"

5. **Artifact uploads**:
   - Upload stress-memory.log (10-hour daemon memory profile)
   - Upload CloudKit-conflict-resolution.log (multi-account test)
   - Upload GroupActivities-state.log (session state diffs)
   - Upload test-results/ (JUnit format for CI visibility)

Per TEST-12: "Nightly stress tests (10-hour daemon, multi-account CloudKit, multi-session Group Activities)."
Per PITFALLS.md note: "Group Activities — requires 2+ physical devices (no simulator)."
Testing: Manually trigger workflow via `gh workflow run nightly-stress.yml`, verify all 3 jobs run (or Job 3 skips with hardware warning).
  </action>
  <verify>
    <automated>
      cat .github/workflows/nightly-stress.yml && gh workflow view nightly-stress
    </automated>
  </verify>
  <done>Nightly stress workflow created, tests daemon memory leak, multi-account CloudKit, multi-session Group Activities, hardware requirement documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Fixture → SwiftData | 100K event fixture loaded into test database (untrusted data volume) |
| Performance CI → Baseline Metrics | CI workflow compares metrics against baseline (tampering risks) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-197-01 | D | 100K event fixture | accept | Test fixture is synthetic data, no user PII, no risk |
| T-197-02 | D | BaselineMetrics.json | mitigate | Commit baseline to git (tamper-evident), sign with gpg if needed |
| T-197-03 | T | Performance regression false positive | mitigate | 2x threshold prevents flakes from noise, 3-run median smooths outliers |
| T-197-04 | R | 10-hour test timeout DoS | mitigate | CI workflow 1h sample, extrapolate to 10h, never run full 10h in CI |
| T-197-05 | I | Metal VRAM detection spoofing | accept | Attacker spoofing VRAM only affects their own app instance, no security impact |

## Adversarial Scenario

Name: Malicious PR injects performance regression disguised as optimization. Attacker submits PR claiming "optimize Decision Timeline query" but actually adds O(n²) loop that slows 100K event load from 2s to 20s. Code review misses regression because small datasets (<1K events) still fast.

Mitigation: CI regression gate runs DecisionTimelinePerformanceTests with 100K fixture on every PR. Load time increases from 2s to 20s, exceeds 2x threshold (4s). Workflow fails, PR blocked. BaselineMetrics.json in git provides tamper-evident reference.
</threat_model>

<verification>
- [ ] PerformanceTests.swift with measure() blocks for spawn, op, timeline
- [ ] MemoryLeakTests.swift with XCTMemoryTest.Heap 1-hour test
- [ ] Baselines documented in PERFORMANCE_BASELINES.md
- [ ] Daemon latency instrumented (spawnLatency, opExecutionTime logged)
- [ ] 100K event fixture generated (decisions + SwiftData)
- [ ] Decision Timeline load test passes <2s threshold
- [ ] MLX-Swift VRAM detection implemented (refuses load <3GB)
- [ ] CI regression gate created (fails on >2x baseline)
- [ ] All tests pass on 8GB M1 MacBook Air (Pitfall 7 verification)
</verification>

<success_criteria>
- [ ] Performance test suite with 15+ tests covering latency, memory, regression
- [ ] Baselines established: spawn<200ms, op<100ms, timeline<500ms (1K events)
- [ ] 10-hour memory leak test passes (<10MB/hour growth threshold)
- [ ] Decision Timeline loads 100K events in <2s (Pitfall 8 prevention)
- [ ] MLX-Swift VRAM detection prevents OOM on 8GB devices (Pitfall 7 prevention)
- [ ] CI regression gate active (blocks merge on >2x slowdown)
- [ ] All tests pass on physical 8GB M1 MacBook Air
</success_criteria>

<output>
After completion, create `.planning/phases/197-performance-testing/197-01-SUMMARY.md` with baseline metrics, memory leak test results, and regression gate configuration.
</output>
