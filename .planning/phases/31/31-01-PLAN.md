# Phase 31 Plan: Validation Integration

**Phase:** 31
**Status:** COMPLETE
**Requirements:** MCPVAL-01, MCPVAL-02

## Summary

Added `erc_check` and `drc_check` MCP convenience tools wrapping existing `run_erc()` and `run_drc()` from `validation/erc_drc.py`.

## What Was Done

- [x] Added `erc_check` meta-tool to `edit_server.py` — wraps `run_erc(sch_path)` returning structured JSON with `passed`, `violation_count`, `errors`, `warnings`, `violations` (capped at 50)
- [x] Added `drc_check` meta-tool to `edit_server.py` — wraps `run_drc(pcb_path)` returning structured JSON with `passed`, `violation_count`, `unconnected_count`, `violations`, `unconnected_items`
- [x] Both tools have `readOnlyHint=True` ToolAnnotations
- [x] Both tools wrap `run_erc`/`run_drc` in `asyncio.to_thread()` for non-blocking execution
- [x] 5 new tests (37 total MCP tests passing)

## Files Changed

| File | Action |
|------|--------|
| `src/kicad_agent/mcp/edit_server.py` | MODIFIED — added erc_check, drc_check tools and dispatch |
| `tests/test_mcp/test_edit_server.py` | MODIFIED — updated counts, added 5 validation tests |

## Test Results

- MCP tests: 37 passed
- Total tool count: 61 (57 ops + 4 meta)
