---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/kicad_agent/validation/roundtrip_regression.py
  - tests/test_roundtrip/test_regression_suite.py
autonomous: true
requirements: [VAL-07]

must_haves:
  truths:
    - "The regression test suite tests all four KiCad file types with multiple sample files of varying complexity"
    - "The regression suite verifies UUID preservation for PCB and footprint files"
    - "The regression suite verifies two-pass stability (deterministic output) for all file types"
    - "Running the full suite with a single command validates the entire parse/serialize pipeline"
    - "Test fixture files are copied into the project to isolate from system KiCad installation changes"
  artifacts:
    - path: "src/kicad_agent/validation/roundtrip_regression.py"
      provides: "Comprehensive regression test runner for all file types"
      exports: ["run_regression_suite", "RegressionResult", "RegressionSuiteResult"]
    - path: "tests/test_roundtrip/test_regression_suite.py"
      provides: "VAL-07 regression test suite"
      contains: "test_full_regression_suite"
    - path: "tests/fixtures/Arduino_Mega/"
      provides: "Copied Arduino_Mega template files for isolated testing"
      contains: ".kicad_sch"
    - path: "tests/fixtures/RaspberryPi/"
      provides: "Copied RaspberryPi template files for second test sample"
      contains: ".kicad_sch"
  key_links:
    - from: "src/kicad_agent/validation/roundtrip_regression.py"
      to: "src/kicad_agent/validation/roundtrip.py"
      via: "Uses round_trip_compare for each file"
      pattern: "from kicad_agent.validation.roundtrip import round_trip_compare"
    - from: "tests/test_roundtrip/test_regression_suite.py"
      to: "src/kicad_agent/validation/roundtrip_regression.py"
      via: "Calls run_regression_suite"
      pattern: "from kicad_agent.validation.roundtrip_regression import run_regression_suite"
---

<objective>
Build a comprehensive round-trip fidelity regression test suite that validates all four KiCad file types against multiple real-world sample files with varying complexity.

Purpose: VAL-07 requires a regression suite that catches parse/serialize regressions before they ship. This suite becomes the gate for all future phases -- no phase passes without this suite being green. The fixtures must be project-local (not dependent on system KiCad installation).

Output: A regression test runner (`roundtrip_regression.py`) that tests all file types, a test suite file, and copied fixture files in `tests/fixtures/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01-02, the regression suite depends on these -->

From src/kicad_agent/validation/roundtrip.py:
```python
@dataclass(frozen=True)
class RoundTripResult:
    is_stable: bool
    original_path: Path
    pass1_path: Path
    pass2_path: Path
    file_type: str
    uuid_preserved: Optional[bool] = None
    error: Optional[str] = None

def round_trip_stable(path: Path, tmp_dir: Path) -> bool: ...
def round_trip_compare(path: Path, tmp_dir: Path) -> RoundTripResult: ...
```

From tests/conftest.py:
```python
FIXTURE_DIR = Path(__file__).parent / "fixtures"
KICAD_TEMPLATES = Path("/Applications/KiCad/KiCad.app/Contents/SharedSupport/template")
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy fixtures and build regression suite</name>
  <files>
    tests/fixtures/Arduino_Mega/Arduino_Mega.kicad_sch,
    tests/fixtures/Arduino_Mega/Arduino_Mega.kicad_pcb,
    tests/fixtures/Arduino_Mega/Arduino_MountingHole.pretty/MountingHole_3.2mm.kicad_mod,
    tests/fixtures/RaspberryPi-uHAT/RaspberryPi-uHAT.kicad_sch,
    tests/fixtures/RaspberryPi-uHAT/RaspberryPi-uHAT.kicad_pcb,
    src/kicad_agent/validation/roundtrip_regression.py,
    tests/test_roundtrip/test_regression_suite.py
  </files>
  <read_first>
    - src/kicad_agent/validation/roundtrip.py (for RoundTripResult and round_trip_compare)
    - tests/conftest.py (for fixture paths)
    - .planning/phases/01-foundation/01-RESEARCH.md lines 519-543 (Wave 0 Gaps and Validation Architecture)
  </read_first>
  <behavior>
    - Test 1: run_regression_suite finds all fixture files and runs round-trip on each
    - Test 2: run_regression_suite returns a RegressionSuiteResult with all files stable
    - Test 3: run_regression_suite reports total_files count >= 5 (at least sch, pcb, mod for two templates plus sym)
    - Test 4: The regression test for each file type reports is_stable=True
    - Test 5: PCB regression tests report uuid_preserved=True
    - Test 6: Running pytest on the regression suite exits with code 0
  </behavior>
  <action>
1. **Copy fixture files into tests/fixtures/**:
   Copy the following files from KiCad templates into the project's `tests/fixtures/` directory:
   - `Arduino_Mega/Arduino_Mega.kicad_sch` (5184 lines, complex schematic)
   - `Arduino_Mega/Arduino_Mega.kicad_pcb` (4040 lines, complex PCB with 115+ UUIDs)
   - `Arduino_Mega/Arduino_MountingHole.pretty/MountingHole_3.2mm.kicad_mod` (simple footprint)
   - `RaspberryPi-uHAT/RaspberryPi-uHAT.kicad_sch` (second schematic for diversity)
   - `RaspberryPi-uHAT/RaspberryPi-uHAT.kicad_pcb` (second PCB for diversity)
   - Find a small .kicad_sym file from `/Applications/KiCad/KiCad.app/Contents/SharedSupport/symbols/` that is under 500 lines and copy it to `tests/fixtures/` (e.g., `Device.kicad_sym` or search for a small one). If Device.kicad_sym is too large, use the Edgeberry.kicad_sym from the templates directory.

   Use shutil.copy2 to preserve file metadata. Create directories as needed.

2. **Update tests/conftest.py**:
   Update the `FIXTURE_DIR` fixture paths to point to the copied local files instead of the system KiCad templates. Keep `KICAD_TEMPLATES` as a reference but change all fixture functions to use local copies:
   - `arduino_mega_sch` -> `FIXTURE_DIR / "Arduino_Mega" / "Arduino_Mega.kicad_sch"`
   - `arduino_mega_pcb` -> `FIXTURE_DIR / "Arduino_Mega" / "Arduino_Mega.kicad_pcb"`
   - `arduino_mounting_hole_mod` -> `FIXTURE_DIR / "Arduino_Mega" / "Arduino_MountingHole.pretty" / "MountingHole_3.2mm.kicad_mod"`
   - `sample_sym_lib` -> `FIXTURE_DIR / "<copied_sym_file>.kicad_sym"`
   Add new fixtures:
   - `raspberry_pi_sch` -> `FIXTURE_DIR / "RaspberryPi-uHAT" / "RaspberryPi-uHAT.kicad_sch"`
   - `raspberry_pi_pcb` -> `FIXTURE_DIR / "RaspberryPi-uHAT" / "RaspberryPi-uHAT.kicad_pcb"`

3. **Create roundtrip_regression.py**:
   ```python
   @dataclass(frozen=True)
   class RegressionResult:
       file_path: Path
       file_type: str
       is_stable: bool
       uuid_preserved: Optional[bool]
       error: Optional[str]

   @dataclass
   class RegressionSuiteResult:
       total_files: int
       passed: int
       failed: int
       results: list[RegressionResult]
       all_passed: bool
   ```

   `run_regression_suite(fixture_dir: Path, tmp_dir: Path) -> RegressionSuiteResult`:
   - Scan `fixture_dir` recursively for files matching `*.kicad_sch`, `*.kicad_pcb`, `*.kicad_sym`, `*.kicad_mod`.
   - For each file, call `round_trip_compare(file, tmp_dir / file.relative_to(fixture_dir))`.
   - Collect results into a `RegressionSuiteResult`.
   - Print a summary: "PASS: X/Y files stable. UUID preservation: X/Y PCB/footprint files."

4. **Create test_regression_suite.py**:
   - `test_full_regression_suite` -- calls `run_regression_suite(FIXTURE_DIR, tmp_path)`, asserts `result.all_passed is True`, asserts `result.total_files >= 5`.
   - `test_schematic_regression` -- runs round-trip specifically on each .kicad_sch fixture.
   - `test_pcb_regression` -- runs round-trip specifically on each .kicad_pcb fixture, asserts `uuid_preserved=True`.
   - `test_footprint_regression` -- runs round-trip on each .kicad_mod fixture.
   - `test_symbol_lib_regression` -- runs round-trip on each .kicad_sym fixture.
  </action>
  <acceptance_criteria>
    - tests/fixtures/Arduino_Mega/Arduino_Mega.kicad_sch exists and is a non-empty file
    - tests/fixtures/Arduino_Mega/Arduino_Mega.kicad_pcb exists and is a non-empty file
    - tests/fixtures/Arduino_Mega/Arduino_MountingHole.pretty/MountingHole_3.2mm.kicad_mod exists
    - tests/fixtures/RaspberryPi-uHAT/RaspberryPi-uHAT.kicad_sch exists
    - tests/fixtures/RaspberryPi-uHAT/RaspberryPi-uHAT.kicad_pcb exists
    - src/kicad_agent/validation/roundtrip_regression.py contains class RegressionSuiteResult with total_files, passed, failed, all_passed attributes
    - src/kicad_agent/validation/roundtrip_regression.py contains function run_regression_suite
    - tests/test_roundtrip/test_regression_suite.py contains test_full_regression_suite
    - python -m pytest tests/test_roundtrip/test_regression_suite.py passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python -m pytest tests/test_roundtrip/test_regression_suite.py -x -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    Fixture files copied into tests/fixtures/. Regression suite discovers all KiCad files, runs round-trip on each, and reports stability results. All files pass the two-pass stability test. PCB and footprint UUID preservation verified. Full suite runs green with `pytest tests/test_roundtrip/ -v`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture files -> regression suite | Fixture files are static test data copied from KiCad templates. Trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Denial of Service | roundtrip_regression.py | accept | Runs on local files only, no external input. Timeout handled by pytest. |
</threat_model>

<verification>
1. Full regression suite passes: `python -m pytest tests/test_roundtrip/test_regression_suite.py -v`
2. All tests together pass: `python -m pytest tests/ -v --tb=short`
3. Fixture files exist in tests/fixtures/: `ls tests/fixtures/Arduino_Mega/`
</verification>

<success_criteria>
- `python -m pytest tests/ -v` passes all tests (parsers, UUID extraction, round-trip stability, regression suite)
- Regression suite tests at least 5 files across all four types
- All PCB/footprint regression tests report uuid_preserved=True
- All files report is_stable=True (two-pass byte-identical)
- Fixture files are project-local, not dependent on system KiCad installation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
