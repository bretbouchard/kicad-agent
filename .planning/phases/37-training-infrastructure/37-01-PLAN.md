---
phase: 37-training-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kicad_agent/logging_config.py
  - src/kicad_agent/mcp/edit_server.py
  - src/kicad_agent/mcp/server.py
  - src/kicad_agent/cli.py
  - tests/test_structured_logging.py
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "configure_logging() at entry points produces structured output from all 70 existing getLogger sites"
    - "KICAD_LOG_FORMAT=json produces valid JSON log lines"
    - "KICAD_LOG_LEVEL=DEBUG changes the root log level"
    - "Console mode produces human-readable colored output"
  artifacts:
    - path: "src/kicad_agent/logging_config.py"
      provides: "configure_logging(level, json_output) function"
      exports: ["configure_logging"]
    - path: "tests/test_structured_logging.py"
      provides: "Tests for logging configuration, JSON mode, stdlib interception"
  key_links:
    - from: "src/kicad_agent/cli.py"
      to: "src/kicad_agent/logging_config.py"
      via: "import and call configure_logging in _handle_collect, main"
      pattern: "configure_logging"
    - from: "src/kicad_agent/mcp/edit_server.py"
      to: "src/kicad_agent/logging_config.py"
      via: "import and call configure_logging in main()"
      pattern: "configure_logging"
    - from: "src/kicad_agent/mcp/server.py"
      to: "src/kicad_agent/logging_config.py"
      via: "import and call configure_logging in main()"
      pattern: "configure_logging"
---

<objective>
Structured logging via structlog with zero per-file changes to the 70 existing getLogger call sites.

Purpose: All kicad-agent processes produce structured, configurable log output. JSON mode for programmatic consumers, console mode for human operators. Per D-01: structlog 25.5.0 with stdlib ProcessorFormatter intercepts all existing logging.getLogger(__name__) calls.

Output: logging_config.py module + wiring in 3 entry points + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-training-infrastructure/37-CONTEXT.md
@.planning/phases/37-training-infrastructure/37-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs from existing code -->

From src/kicad_agent/cli.py:
- main(argv) is the CLI entry point
- _handle_collect(argv) calls logging.basicConfig (line 159-162)
- No other logging config in main() -- relies on default

From src/kicad_agent/mcp/edit_server.py:
- main() calls logging.basicConfig(level=logging.INFO) at line 492
- _run_server() runs the async MCP server

From src/kicad_agent/mcp/server.py:
- main() at line 277 calls asyncio.run(_run_server())
- No logging.basicConfig call currently -- uses default

From RESEARCH Pattern 3 (verified structlog pattern):
```python
import structlog

def configure_logging(level="INFO", json_output=False):
    # Sets up structlog.stdlib.ProcessorFormatter
    # Intercepts all stdlib logging automatically
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create logging_config.py and tests</name>
  <files>src/kicad_agent/logging_config.py, tests/test_structured_logging.py</files>
  <behavior>
    - Test configure_logging(level="DEBUG") sets root logger level to DEBUG
    - Test configure_logging(json_output=True) produces valid JSON log lines when a stdlib logger emits
    - Test configure_logging(json_output=False) produces console-formatted output (not JSON)
    - Test calling configure_logging twice is idempotent (no duplicate handlers, no errors)
    - Test a module-level logging.getLogger(__name__) call produces structured output after configure
  </behavior>
  <action>
    Create src/kicad_agent/logging_config.py implementing configure_logging per D-01:

    1. Read KICAD_LOG_LEVEL env var (default "INFO"), map to logging level int
    2. Read KICAD_LOG_FORMAT env var (default "console"), map json_output bool
    3. Accept optional level and json_output kwargs that override env vars
    4. Call logging.basicConfig(format="%(message)s", level=log_level, force=True)
    5. Configure structlog with processors: contextvars merge, filter_by_level, add_logger_name, add_log_level, PositionalArgumentsFormatter, TimeStamper(fmt="iso"), StackInfoRenderer, format_exc_info, UnicodeDecoder, ProcessorFormatter.wrap_for_formatter
    6. Use structlog.stdlib.LoggerFactory() and BoundLogger wrapper class
    7. Set cache_logger_on_first_use=True
    8. Create structlog.stdlib.ProcessorFormatter with JSONRenderer or ConsoleRenderer
    9. Clear root.handlers, add single StreamHandler with the ProcessorFormatter

    Create tests/test_structured_logging.py:
    - test_configure_sets_level: configure_logging(level="DEBUG"), assert logging.getLogger().level == logging.DEBUG
    - test_json_output_valid_json: configure_logging(json_output=True), emit with getLogger, capture stderr, parse as JSON
    - test_console_output_not_json: configure_logging(json_output=False), emit with getLogger, assert output is not JSON-parseable (contains ANSI or plain text)
    - test_idempotent: call configure_logging twice, assert len(root.handlers) == 1
    - test_stdlib_interception: configure_logging(json_output=True), use logging.getLogger("test.module"), emit INFO, verify output is structured JSON with "test.module" as event or logger_name

    Use pytest capsys or io.StringIO to capture log output. Reset structlog and logging between tests via fixture.
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python -m pytest tests/test_structured_logging.py -x -v</automated>
  </verify>
  <done>
    - configure_logging() sets up structlog with ProcessorFormatter
    - JSON mode produces valid JSON log lines
    - Console mode produces human-readable output
    - Idempotent -- no duplicate handlers on repeated calls
    - stdlib getLogger sites produce structured output
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire configure_logging into all entry points</name>
  <files>src/kicad_agent/cli.py, src/kicad_agent/mcp/edit_server.py, src/kicad_agent/mcp/server.py</files>
  <action>
    Replace logging.basicConfig calls with configure_logging() in all 3 entry points:

    1. src/kicad_agent/cli.py:
       - Add "from kicad_agent.logging_config import configure_logging" at top
       - In _handle_collect() (line ~159): Replace logging.basicConfig(level=logging.INFO, format=...) with configure_logging()
       - In main() (top of function, before any other work): Add configure_logging() call -- this covers the legacy operation mode

    2. src/kicad_agent/mcp/edit_server.py:
       - Add "from kicad_agent.logging_config import configure_logging" at top
       - In main() (line ~492): Replace logging.basicConfig(level=logging.INFO) with configure_logging()

    3. src/kicad_agent/mcp/server.py:
       - Add "from kicad_agent.logging_config import configure_logging" at top
       - In main() (line ~277): Add configure_logging() before asyncio.run()

    Do NOT change any of the 70 files that use logging.getLogger(__name__) -- structlog's ProcessorFormatter intercepts them automatically.
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python -m pytest tests/test_structured_logging.py -x && python -c "from kicad_agent.logging_config import configure_logging; configure_logging()" && echo "OK"</automated>
  </verify>
  <done>
    - All 3 entry points call configure_logging()
    - No remaining logging.basicConfig calls in entry points
    - Existing getLogger(__name__) sites across all 70 files automatically produce structured output
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars -> logging config | KICAD_LOG_LEVEL and KICAD_LOG_FORMAT read from environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Spoofing | logging_config.py | mitigate | Structured JSON logging prevents log injection/forging -- key-value pairs are parsed, not free-text |
| T-37-02 | Tampering | env var input | accept | Log level strings are validated via getattr(logging, level.upper(), logging.INFO) -- invalid values default to INFO |
| T-37-03 | Information Disclosure | debug logging | mitigate | DEBUG level is opt-in via env var, not default. Default is INFO. Production deployments should not set DEBUG |
</threat_model>

<verification>
1. All tests pass: `python -m pytest tests/test_structured_logging.py -x`
2. Logging config importable: `python -c "from kicad_agent.logging_config import configure_logging; configure_logging()"`
3. No logging.basicConfig remains in entry points: `grep -r "logging.basicConfig" src/kicad_agent/cli.py src/kicad_agent/mcp/edit_server.py src/kicad_agent/mcp/server.py` returns zero results
</verification>

<success_criteria>
- configure_logging() produces structured output from all 70 existing getLogger call sites
- KICAD_LOG_FORMAT=json produces valid JSON log lines parseable by jq or any JSON parser
- KICAD_LOG_LEVEL=DEBUG changes the root log level
- Console mode produces human-readable output (no JSON)
- Idempotent -- repeated calls do not duplicate handlers or cause errors
- All 3 entry points (cli.py main, edit_server.py main, server.py main) wired
</success_criteria>

<output>
After completion, create `.planning/phases/37-training-infrastructure/37-01-SUMMARY.md`
</output>
