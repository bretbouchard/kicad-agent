---
phase: 231
plan: 01
title: Make Swift NativeERC the primary validation source
type: execute
wave: 1
depends_on: ["234B"]
autonomous: true
files_modified:
  - macos-app/Sources/KiCadAgent/Views/Chat/ChatView.swift
  - macos-app/Sources/KiCadAgent/Validation/
  - tests/eval/
must_haves:
  - ChatView surfaces Swift NativeERC results (not Python daemon)
  - Native ERC panel promoted to primary status
  - Python daemon ERC marked as fallback
---

# Plan 231-01: Wire Swift ERC as Primary Validation

<objective>
The Swift NativeERC engine (Phase 218) is more comprehensive than the
Python daemon's native_erc (per Phase 234B parity: Swift has 4x more
violations detected). Promote Swift to primary validation source in the
chat pipeline; demote Python to fallback.

## Approach

1. Audit `macos-app/Sources/KiCadAgent/Validation/` — confirm Swift engine
   is wired into the validation panel (Phase 218)
2. Update ChatView to display Swift ERC results first
3. Mark Python daemon ERC as fallback in UI (e.g., "Python fallback
   engine" subtitle)
4. Add integration test that exercises both paths

## Status

Phase 218 native ERC engine shipped 68 checks; Phase 234B confirmed
Swift > Python coverage. UI plumbing already exists. Promotion work:
small.

This phase can be completed in 1-2 days.