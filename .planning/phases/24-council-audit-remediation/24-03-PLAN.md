---
phase: 24-council-audit-remediation
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - src/kicad_agent/ops/schema.py
  - src/kicad_agent/ops/_schema_component.py
  - src/kicad_agent/ops/_schema_net.py
  - src/kicad_agent/ops/_schema_reference.py
  - src/kicad_agent/ops/_schema_footprint.py
  - src/kicad_agent/ops/_schema_wire.py
  - src/kicad_agent/ops/_schema_library.py
  - src/kicad_agent/ops/_schema_pcb.py
  - src/kicad_agent/ops/_schema_validation.py
  - src/kicad_agent/ops/_schema_create.py
  - src/kicad_agent/ops/_schema_repair.py
  - src/kicad_agent/ops/executor.py
  - src/kicad_agent/format_convert.py
  - src/kicad_agent/inference/best_of_n.py
  - src/kicad_agent/training/templates.py
  - src/kicad_agent/handler.py
  - src/kicad_agent/ops/repair.py
  - tests/test_code_quality.py
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "schema.py is under 800 lines (split into sub-modules with re-exports)"
    - "All broad except Exception catches are narrowed to specific types"
    - "_SAFE_ID_PATTERN extracted into a shared validators module"
    - "Dead code removed: _fix_sheet_instances, n_complete parameter, unused templates"
    - "Production assertions replaced with proper error handling"
    - "Module-level imports (no function-level import re)"
    - "Docstrings accurately describe what functions do"
  artifacts:
    - path: "src/kicad_agent/ops/schema.py"
      provides: "Re-export hub for all Op classes, under 200 lines"
      max_lines: 200
    - path: "src/kicad_agent/ops/_schema_component.py"
      provides: "Component operation schemas"
      contains: "AddComponentOp"
    - path: "src/kicad_agent/ops/_schema_net.py"
      provides: "Net operation schemas"
      contains: "AddNetOp"
    - path: "src/kicad_agent/ops/_schema_reference.py"
      provides: "Reference operation schemas"
      contains: "RenumberRefsOp"
    - path: "src/kicad_agent/ops/_schema_footprint.py"
      provides: "Footprint operation schemas"
      contains: "AssignFootprintOp"
    - path: "src/kicad_agent/ops/_schema_wire.py"
      provides: "Wire/label/power operation schemas"
      contains: "AddWireOp"
    - path: "src/kicad_agent/ops/_schema_library.py"
      provides: "Library operation schemas"
      contains: "AddLibEntryOp"
    - path: "src/kicad_agent/ops/_schema_pcb.py"
      provides: "PCB-specific operation schemas"
      contains: "AddCopperZoneOp"
    - path: "src/kicad_agent/ops/_schema_validation.py"
      provides: "Validation operation schemas"
      contains: "ValidatePowerNetsOp"
    - path: "src/kicad_agent/ops/_schema_create.py"
      provides: "File creation operation schemas"
      contains: "CreateSchematicOp"
    - path: "src/kicad_agent/ops/_schema_repair.py"
      provides: "Repair operation schemas"
      contains: "RepairSchematicOp"
  key_links:
    - from: "src/kicad_agent/ops/schema.py"
      to: "_schema_*.py sub-modules"
      via: "re-import and re-export"
      pattern: "from.*_schema_.*import"
---

<objective>
Improve code quality by splitting the oversized schema, narrowing exception catches, removing dead code, and consolidating duplication.

Purpose: schema.py at 1381 lines exceeds the 800-line limit. 79 broad `except Exception` catches hide real errors. Dead code and duplicated patterns create maintenance burden. This plan makes the codebase maintainable and SLC-compliant.

Output: Schema split into 10 sub-modules, narrowed exception catches, dead code removed, validators consolidated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-council-audit-remediation/24-CONTEXT.md
@.planning/phases/24-council-audit-remediation/24-RESEARCH.md
@COUNCIL-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split schema.py into sub-modules by operation category (M-9, M-11)</name>
  <files>
    src/kicad_agent/ops/schema.py,
    src/kicad_agent/ops/_schema_component.py,
    src/kicad_agent/ops/_schema_net.py,
    src/kicad_agent/ops/_schema_reference.py,
    src/kicad_agent/ops/_schema_footprint.py,
    src/kicad_agent/ops/_schema_wire.py,
    src/kicad_agent/ops/_schema_library.py,
    src/kicad_agent/ops/_schema_pcb.py,
    src/kicad_agent/ops/_schema_validation.py,
    src/kicad_agent/ops/_schema_create.py,
    src/kicad_agent/ops/_schema_repair.py,
    tests/test_code_quality.py
  </files>
  <read_first>
    src/kicad_agent/ops/schema.py (full file -- understand all classes, imports, validators, the Operation union, get_operation_schema()),
  </read_first>
  <action>
    **M-9: Split schema.py into sub-modules**

    Follow the structure from the RESEARCH.md recommended project layout. The main schema.py becomes a re-export hub.

    1. Keep in `schema.py` (the hub file):
       - All imports (pydantic, re, typing, etc.)
       - `TargetFile` model
       - `PositionSpec` model
       - `_SAFE_ID_PATTERN` and validators (M-11: make this the canonical location)
       - `_UNSAFE_SEXPR_CHARS` and `_validate_sexpr_safe_string` (added in Plan 01)
       - `Operation` discriminated union (import Op classes from sub-modules)
       - `get_operation_schema()` function
       - `__all__` export list

    2. Create sub-modules, each importing `TargetFile`, `PositionSpec`, `_SAFE_ID_PATTERN`, `_UNSAFE_SEXPR_CHARS`, `_validate_sexpr_safe_string` from the main `schema.py`:
       - `_schema_component.py`: AddComponentOp, RemoveComponentOp, MoveComponentOp, ModifyPropertyOp, DuplicateComponentOp, ArrayReplicateOp
       - `_schema_net.py`: AddNetOp, RemoveNetOp, RenameNetOp
       - `_schema_reference.py`: RenumberRefsOp, ValidateRefsOp, AnnotateOp, CrossRefCheckOp
       - `_schema_footprint.py`: AssignFootprintOp, SwapFootprintOp, ValidateFootprintOp, VerifyPinMapOp, UpdateFootprintFromLibraryOp
       - `_schema_wire.py`: AddWireOp, AddLabelOp, AddPowerOp, AddNoConnectOp, AddJunctionOp
       - `_schema_library.py`: AddLibEntryOp, RemoveLibEntryOp
       - `_schema_pcb.py`: AddNetClassOp, AddDesignRuleOp, AddCopperZoneOp, SetBoardOutlineOp, AssignNetClassOp, AutoRouteOp
       - `_schema_validation.py`: ValidatePowerNetsOp, ValidateSchematicOp, ParseErcOp, ExtractViolationPositionsOp, ValidateHlabelsOp
       - `_schema_create.py`: CreateSchematicOp, CreatePcbOp, CreateProjectOp, CreateSymbolOp, EmbedSymbolOp
       - `_schema_repair.py`: RepairSchematicOp, ConvertKicad6To10Op, SnapToGridOp, AddPowerFlagOp, RebuildRootSheetOp, SwapSymbolOp

    3. In the main `schema.py`, import and re-export all Op classes:
       ```python
       from kicad_agent.ops._schema_component import (AddComponentOp, RemoveComponentOp, ...)
       from kicad_agent.ops._schema_net import (AddNetOp, RemoveNetOp, RenameNetOp)
       # ... etc
       ```

    4. The `Operation` union stays in schema.py, referencing the imported classes.

    5. **CRITICAL:** External code must NOT need to change imports. Everything must still be importable from `kicad_agent.ops.schema`. Verify by running the full test suite.

    **M-11: Consolidate _SAFE_ID_PATTERN**
    After the split, `_SAFE_ID_PATTERN` lives only in the main `schema.py` as the canonical source. Remove any duplicates from other files (grep for `_SAFE_ID_PATTERN` across the codebase and update imports to reference `kicad_agent.ops.schema._SAFE_ID_PATTERN`).

    **Tests: Create test_code_quality.py**
    1. Test that `from kicad_agent.ops.schema import AddComponentOp` still works
    2. Test that `from kicad_agent.ops.schema import Operation` still works
    3. Test that Operation union includes all expected op_types
    4. Test that schema.py is under 200 lines (after split)
    5. Test that each sub-module has the expected classes
  </action>
  <acceptance_criteria>
    - schema.py is under 200 lines (just hub, union, validators, re-exports)
    - 10 new _schema_*.py files exist in src/kicad_agent/ops/
    - `from kicad_agent.ops.schema import AddComponentOp` works (no ImportError)
    - `from kicad_agent.ops.schema import Operation` works
    - Operation discriminated union includes all non-bus op_types
    - get_operation_schema() still works and returns correct schema
    - _SAFE_ID_PATTERN defined only in schema.py (no duplicates elsewhere)
    - All 1479+ existing tests pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python -m pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - schema.py split into 10 sub-modules, main file is a re-export hub under 200 lines
    - All external imports continue to work without changes
    - _SAFE_ID_PATTERN consolidated to single canonical location
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead code, narrow exception catches, fix misc quality issues (M-10, M-12, M-13, M-20, L-4 through L-9)</name>
  <files>
    src/kicad_agent/ops/executor.py,
    src/kicad_agent/format_convert.py,
    src/kicad_agent/inference/best_of_n.py,
    src/kicad_agent/training/templates.py,
    src/kicad_agent/handler.py,
    src/kicad_agent/ops/repair.py,
    tests/test_code_quality.py
  </files>
  <read_first>
    src/kicad_agent/format_convert.py (find _fix_sheet_instances function),
    src/kicad_agent/inference/best_of_n.py (full file -- find n_complete parameter, assert statement),
    src/kicad_agent/training/templates.py (full file -- find all template definitions, identify unused ones),
    src/kicad_agent/handler.py (full file -- find docstring at top, exception catches),
    src/kicad_agent/ops/repair.py (lines 195-215 for dead net-short detection loop),
    src/kicad_agent/ops/executor.py (find all `except Exception` patterns)
  </read_first>
  <action>
    **M-12: Remove no-op _fix_sheet_instances from format_convert.py**
    Find and remove the `_fix_sheet_instances` function entirely. It is a no-op that does nothing. Remove any calls to it.

    **M-13: Remove unused n_complete parameter from best_of_n_select**
    In `best_of_n.py`, find the `best_of_n_select` function and remove the `n_complete` parameter. Update any callers.

    **L-4: Replace assert with proper error handling in best_of_n.py**
    Find `assert best is not None` (or similar assertion) and replace with:
    ```python
    if best is None:
        raise ValueError("best_of_n_select: no valid chain found")
    ```

    **M-20: Remove unused task templates from templates.py**
    Identify the 3 unused templates (the ones not referenced by any other code). Remove them. Keep templates that are actively used by the training pipeline.

    **L-6: Remove repeated import dataclasses in executor.py**
    Find all instances of `import dataclasses` inside function bodies in executor.py. Move to module-level import at the top of the file (a single `import dataclasses`). Remove the function-level imports.

    **L-7: Fix redundant exception catch**
    Find `except (ValueError, Exception)` -- ValueError is a subclass of Exception, so the tuple is redundant. Replace with `except Exception` or narrow to just `except ValueError` depending on context. Check pipeline.py line 122 specifically.

    **L-9: Fix handler.py docstring**
    The handler.py docstring says "does NOT execute mutations" but it does execute them via the executor. Fix the docstring to accurately describe what it does.

    **M-10: Narrow broad exception catches (priority: security-relevant paths)**
    Focus on the top 20-30 most important catches in these files:
    - `handler.py` -- narrow to specific types (ValueError, FileNotFoundError, ValidationError, NotImplementedError)
    - `executor.py` -- narrow to specific types
    - `cli.py` -- narrow to specific types
    - `ops/repair.py` -- narrow to specific types

    For each catch:
    1. Read the try block to determine which exceptions are actually raised
    2. Replace `except Exception` with the specific types
    3. Keep a final `except Exception` as a safety net only if needed, with a comment explaining why

    Do NOT attempt to narrow all 79 catches in one pass. Focus on the highest-value ones. The rest can be addressed in ongoing improvement.

    **Add tests to test_code_quality.py**
    1. Test that `_fix_sheet_instances` no longer exists in format_convert
    2. Test that `best_of_n_select` raises ValueError when no valid chain found (instead of AssertionError)
    3. Test that handler.py docstring does not say "does NOT execute"
    4. Test schema.py is under 200 lines
  </action>
  <acceptance_criteria>
    - format_convert.py does NOT contain `_fix_sheet_instances`
    - best_of_n.py does NOT have `n_complete` parameter in `best_of_n_select`
    - best_of_n.py does NOT contain `assert` in production logic
    - best_of_n.py raises ValueError when no valid chain found
    - templates.py has unused templates removed
    - executor.py has a single module-level `import dataclasses` (no function-level imports)
    - No `except (ValueError, Exception)` patterns exist anywhere
    - handler.py docstring accurately describes that it executes mutations
    - handler.py, executor.py, cli.py, repair.py have narrowed exception catches
    - tests/test_code_quality.py exists with 4+ test functions
    - All 1479+ existing tests pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python -m pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Dead code removed (_fix_sheet_instances, n_complete, unused templates)
    - Production assertions replaced with proper ValueError
    - Repeated imports consolidated
    - Redundant exception catches fixed
    - Handler docstring accurate
    - Top ~25 broad exception catches narrowed to specific types
    - All existing tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal code quality | No trust boundary changes -- internal refactoring only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-12 | Tampering | Exception narrowing | accept | Safety net catches preserved where needed |
| T-24-13 | Denial of Service | Schema split | accept | Import compatibility verified by test suite |
</threat_model>

<verification>
1. All existing tests pass: `python -m pytest tests/ -x -q --tb=short`
2. Code quality tests pass: `python -m pytest tests/test_code_quality.py -v`
3. Schema split works: `python -c "from kicad_agent.ops.schema import Operation; print(len(Operation.__args__))"` returns correct count
4. Dead code removed: `grep -c "_fix_sheet_instances" src/kicad_agent/format_convert.py` returns 0
5. No broad catches in priority files: `grep -c "except Exception" src/kicad_agent/handler.py` significantly reduced
</verification>

<success_criteria>
- schema.py split into 10 sub-modules, main file under 200 lines
- All external imports continue to work
- _SAFE_ID_PATTERN consolidated to single canonical location
- Dead code removed (_fix_sheet_instances, n_complete, unused templates)
- Production assertions replaced with proper error handling
- Top ~25 broad exception catches narrowed
- Handler docstring accurate
- All 1479+ existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-council-audit-remediation/24-03-SUMMARY.md`
</output>
