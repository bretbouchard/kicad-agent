---
phase: 24-council-audit-remediation
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/kicad_agent/ops/schema.py
  - src/kicad_agent/ops/executor.py
  - skills/prompt.md
  - skills/SKILL.md
  - README.md
  - tests/test_slc_compliance.py
autonomous: true
requirements:
  - SLC-01
  - SLC-02
  - SLC-03

must_haves:
  truths:
    - "add_bus and remove_bus are completely removed from schema, executor, prompt.md, README, and SKILL.md"
    - "validate_footprint performs actual library lookup instead of always returning valid=True"
    - "place_no_connects_from_erc is removed from prompt.md and README"
    - "Operation counts in SKILL.md, README, and prompt.md match the actual schema count"
    - "All prompt.md field names match schema.py field names exactly"
  artifacts:
    - path: "src/kicad_agent/ops/schema.py"
      provides: "Schema without AddBusOp/RemoveBusOp"
      contains: "ValidateFootprintOp"
      not_contains: "AddBusOp"
    - path: "src/kicad_agent/ops/executor.py"
      provides: "Executor without bus stubs, with real validate_footprint"
      not_contains: "NotImplementedError"
    - path: "skills/prompt.md"
      provides: "Prompt matching schema exactly"
      not_contains: "place_no_connects_from_erc"
    - path: "skills/SKILL.md"
      provides: "Accurate operation count"
    - path: "tests/test_slc_compliance.py"
      provides: "SLC regression tests"
      min_lines: 60
  key_links:
    - from: "src/kicad_agent/ops/schema.py"
      to: "Operation union"
      via: "discriminated union must exclude bus ops"
      pattern: "Operation\\s*=.*Union"
    - from: "src/kicad_agent/ops/executor.py"
      to: "src/kicad_agent/project/lib_table.py"
      via: "validate_footprint uses fp-lib-table lookup"
      pattern: "parse_lib_table"
---

<objective>
Fix all 3 SLC violations and reconcile documentation with implementation.

Purpose: Remove stubs that advertise capability but crash at runtime (bus operations), implement actual validation for validate_footprint, remove phantom operations from documentation, and ensure all documentation (prompt.md, SKILL.md, README) accurately reflects the actual schema.

Output: Clean schema without bus stubs, working validate_footprint with library lookup, documentation consistent with implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-council-audit-remediation/24-CONTEXT.md
@.planning/phases/24-council-audit-remediation/24-RESEARCH.md
@COUNCIL-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove bus operation stubs and phantom operations, fix validate_footprint (C-2, C-3, C-5)</name>
  <files>
    src/kicad_agent/ops/schema.py,
    src/kicad_agent/ops/executor.py,
    tests/test_slc_compliance.py
  </files>
  <read_first>
    src/kicad_agent/ops/schema.py (lines 423-491 for AddBusOp/RemoveBusOp classes, lines 1410-1455 for Operation union),
    src/kicad_agent/ops/executor.py (lines 228-235 for bus handler stubs, lines 178-182 and 383-387 for validate_footprint stubs),
    src/kicad_agent/project/lib_table.py (full file -- understand parse_lib_table and LibTable API for C-3 implementation),
    src/kicad_agent/lib_resolver.py (full file -- understand footprint resolution logic)
  </read_first>
  <action>
    **C-2: Remove AddBusOp and RemoveBusOp from schema**
    1. Delete the `class AddBusOp(BaseModel)` block (schema.py lines 423-464) entirely
    2. Delete the `class RemoveBusOp(BaseModel)` block (schema.py lines 468-491) entirely
    3. Remove `| AddBusOp` and `| RemoveBusOp` from the Operation discriminated union (lines 1418-1419)
    4. Remove any imports or references to AddBusOp/RemoveBusOp elsewhere in schema.py

    **C-2: Remove bus handlers from executor**
    1. Delete the `@register_schematic("add_bus")` decorator and `_handle_add_bus` function (executor.py lines 228-230)
    2. Delete the `@register_schematic("remove_bus")` decorator and `_handle_remove_bus` function (executor.py lines 233-235)

    **C-3: Implement real validate_footprint**
    Replace both validate_footprint stubs (schematic handler at line 180-182, PCB handler at line 385-387) with actual library lookup logic.

    The implementation should:
    1. Parse the `footprint_lib_id` field (format: "LibraryNickname:FootprintName")
    2. Split on `:` to get library nickname and footprint name
    3. Find the project's fp-lib-table (look in same directory as the target file, then parent directories up to project root)
    4. Parse the fp-lib-table using `parse_lib_table` from `kicad_agent.project.lib_table`
    5. Find the library entry matching the nickname
    6. Check if the footprint .kicad_mod file exists in the library's path
    7. Return `{"footprint_lib_id": op.footprint_lib_id, "valid": True/False, "library_path": str}` if found, `{"footprint_lib_id": op.footprint_lib_id, "valid": False, "error": "..."}` if not

    If the fp-lib-table cannot be found or parsed, return `{"footprint_lib_id": op.footprint_lib_id, "valid": False, "error": "fp-lib-table not found"}` -- do NOT silently return True.

    **C-5: Remove phantom place_no_connects_from_erc** -- this is in prompt.md only (no schema/executor), so it's handled in Task 2 documentation fixes.

    **Tests: Create test_slc_compliance.py**
    1. Test AddBusOp is not importable from schema (import attempt raises AttributeError or the class does not exist)
    2. Test RemoveBusOp is not importable from schema
    3. Test Operation union does not include bus operation types (validate that `Operation` discriminated union rejects op_type="add_bus")
    4. Test validate_footprint returns valid=True for a known good footprint lib_id
    5. Test validate_footprint returns valid=False for a non-existent footprint
    6. Test validate_footprint returns valid=False when fp-lib-table is missing
  </action>
  <acceptance_criteria>
    - schema.py does NOT contain `class AddBusOp`
    - schema.py does NOT contain `class RemoveBusOp`
    - schema.py Operation union does NOT reference `AddBusOp` or `RemoveBusOp`
    - executor.py does NOT contain `add_bus` or `remove_bus` handler registrations
    - executor.py does NOT contain `NotImplementedError` for bus operations
    - executor.py validate_footprint handlers perform actual library lookup
    - executor.py imports `parse_lib_table` from `kicad_agent.project.lib_table`
    - tests/test_slc_compliance.py exists with 6+ test functions
    - All 1479+ existing tests pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python -m pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - AddBusOp and RemoveBusOp completely removed from schema and executor
    - validate_footprint performs actual fp-lib-table lookup instead of always returning True
    - All bus operation tests verify the removal
    - All existing tests continue to pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix prompt-schema mismatches, remove phantom docs, reconcile operation counts (C-5, H-6, H-9)</name>
  <files>
    skills/prompt.md,
    skills/SKILL.md,
    README.md,
    tests/test_slc_compliance.py
  </files>
  <read_first>
    skills/prompt.md (full file -- find all operation documentation sections, field names, place_no_connects_from_erc references),
    skills/SKILL.md (full file -- find operation count claims),
    README.md (full file -- find operation count claims, training section),
    src/kicad_agent/ops/schema.py (scan for exact field names in SnapToGridOp, all operation class names to get true count)
  </read_first>
  <action>
    **C-5: Remove phantom place_no_connects_from_erc from prompt.md**
    Search prompt.md for any section documenting `place_no_connects_from_erc`. This operation has no schema class and no executor handler. Remove the entire documentation section for it (description, parameters, examples).

    **C-5: Remove phantom place_no_connects_from_erc from README**
    Search README.md for references to `place_no_connects_from_erc` and remove them.

    **H-6: Fix prompt.md field name mismatches**
    1. Find `snap_to_grid` section in prompt.md. Change field name `grid_size` to `grid_mm` to match `SnapToGridOp` in schema.py (which has `grid_mm: float`)
    2. Search prompt.md for all references to `erc_report_path` field. If the schema does not have this field in the corresponding Op class, remove it from the prompt documentation.
    3. Do a systematic comparison: for each operation documented in prompt.md, verify all field names match the Pydantic model in schema.py. Fix any mismatches found.

    **H-9: Reconcile operation counts across all documentation**
    1. Count the actual number of operation classes in schema.py (after removing AddBusOp/RemoveBusOp in Task 1). This is the ground truth.
    2. Update SKILL.md operation count to match the actual count.
    3. Update README.md operation count to match the actual count.
    4. Update prompt.md -- if it lists an operation count, update to match.

    **Add SLC documentation tests**
    Add to test_slc_compliance.py:
    1. Test that prompt.md does NOT contain `place_no_connects_from_erc`
    2. Test that prompt.md uses `grid_mm` (not `grid_size`) for snap_to_grid
    3. Test that the operation count in SKILL.md matches the number of Op classes in schema.py
    4. Test that the operation count in README.md matches the number of Op classes in schema.py
  </action>
  <acceptance_criteria>
    - prompt.md does NOT contain `place_no_connects_from_erc`
    - README.md does NOT contain `place_no_connects_from_erc`
    - prompt.md `snap_to_grid` section uses `grid_mm` (not `grid_size`)
    - prompt.md does NOT reference `erc_report_path` for operations where schema lacks it
    - SKILL.md operation count matches actual Op class count in schema.py
    - README.md operation count matches actual Op class count in schema.py
    - tests/test_slc_compliance.py includes documentation consistency tests
    - All 1479+ existing tests pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python -m pytest tests/test_slc_compliance.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - place_no_connects_from_erc removed from prompt.md and README
    - prompt.md field names match schema.py exactly (grid_mm, no erc_report_path mismatches)
    - Operation counts consistent across SKILL.md, README.md, and prompt.md
    - All documentation consistency tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema -> LLM Agent | Prompt.md is the contract LLMs use to generate operations |
| Documentation -> User | README and SKILL.md set user expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | Tampering | validate_footprint | mitigate | Actual library lookup instead of always-True (C-3) |
| T-24-10 | Spoofing | prompt.md field names | mitigate | Field names validated against schema.py (H-6) |
| T-24-11 | Repudiation | SKILL.md/README counts | accept | Count consistency tests detect drift |
</threat_model>

<verification>
1. All existing tests pass: `python -m pytest tests/ -x -q --tb=short`
2. SLC compliance tests pass: `python -m pytest tests/test_slc_compliance.py -v`
3. Schema.py has no AddBusOp or RemoveBusOp classes: `grep -c "class AddBusOp" src/kicad_agent/ops/schema.py` returns 0
4. Prompt.md has no place_no_connects_from_erc: `grep -c "place_no_connects_from_erc" skills/prompt.md` returns 0
5. Operation counts consistent: SKILL.md count == README.md count == schema.py Op class count
</verification>

<success_criteria>
- Bus operations (AddBusOp, RemoveBusOp) completely removed from schema and executor
- validate_footprint performs actual fp-lib-table lookup, returns False for unknown footprints
- place_no_connects_from_erc removed from all documentation
- prompt.md field names match schema.py exactly
- Operation counts consistent across SKILL.md, README.md, prompt.md
- All 1479+ existing tests pass
- New test file test_slc_compliance.py validates all fixes
</success_criteria>

<output>
After completion, create `.planning/phases/24-council-audit-remediation/24-02-SUMMARY.md`
</output>
