---
phase: 24-council-audit-remediation
plan: 04
type: execute
wave: 4
depends_on: ["24-03"]
files_modified:
  - src/kicad_agent/ops/repair.py
  - src/kicad_agent/training/grpo.py
  - src/kicad_agent/training/grpo_trainer.py
  - src/kicad_agent/training/pipeline.py
  - src/kicad_agent/training/reward_model.py
  - src/kicad_agent/training/sft/trainer.py
  - src/kicad_agent/training/templates.py
  - src/kicad_agent/training/evaluation.py
  - src/kicad_agent/inference/best_of_n.py
  - tests/test_training_integrity.py
  - tests/test_executor_ops.py
  - tests/test_serializer.py
  - tests/test_e2e_pipeline.py
autonomous: true
requirements:
  - QUAL-02
  - TEST-01

must_haves:
  truths:
    - "Training data split into separate train/eval sets (no circular evaluation)"
    - "Dead GRPO implementation removed, active one preserved"
    - "PPO clip applies to probability ratios, not raw advantages"
    - "Validation loss computed during reward model training"
    - "Best-of-N raises error or returns None when reward model unavailable"
    - "Dead repair code in net-short detection loop has actual logic"
    - "Core schematic ops have executor tests"
    - "Serializer modules have direct unit tests"
    - "End-to-end integration test validates full pipeline"
  artifacts:
    - path: "tests/test_training_integrity.py"
      provides: "Training pipeline math and data integrity tests"
      min_lines: 80
    - path: "tests/test_executor_ops.py"
      provides: "Core schematic operation executor tests"
      min_lines: 60
    - path: "tests/test_serializer.py"
      provides: "Serializer module unit tests"
      min_lines: 40
    - path: "tests/test_e2e_pipeline.py"
      provides: "End-to-end integration test"
      min_lines: 50
  key_links:
    - from: "src/kicad_agent/training/pipeline.py"
      to: "src/kicad_agent/training/grpo.py"
      via: "pipeline imports the active GRPO implementation"
      pattern: "from.*grpo.*import"
    - from: "tests/test_e2e_pipeline.py"
      to: "src/kicad_agent/handler.py"
      via: "handle_operation() end-to-end"
      pattern: "handle_operation"
---

<objective>
Fix training pipeline integrity issues and add missing test coverage for core operations.

Purpose: The training pipeline has mathematical errors (PPO clip applied to wrong values, circular evaluation, dead KL divergence code) and the core editing operations lack test coverage. This plan fixes the math, removes dead training code, implements actual repair logic, and adds the tests that should exist for the core value proposition.

Output: Corrected training pipeline, working repair code, comprehensive test coverage for executor, serializer, and end-to-end flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-council-audit-remediation/24-CONTEXT.md
@.planning/phases/24-council-audit-remediation/24-RESEARCH.md
@COUNCIL-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix training pipeline integrity issues (H-10, H-11, M-14 through M-19, L-16)</name>
  <files>
    src/kicad_agent/training/grpo.py,
    src/kicad_agent/training/grpo_trainer.py,
    src/kicad_agent/training/pipeline.py,
    src/kicad_agent/training/reward_model.py,
    src/kicad_agent/training/sft/trainer.py,
    src/kicad_agent/training/templates.py,
    src/kicad_agent/training/evaluation.py,
    src/kicad_agent/inference/best_of_n.py,
    tests/test_training_integrity.py
  </files>
  <read_first>
    src/kicad_agent/training/pipeline.py (full file -- determine which GRPO implementation is actually imported and used),
    src/kicad_agent/training/grpo.py (full file -- understand low-level GRPO loop, find dead KL divergence code),
    src/kicad_agent/training/grpo_trainer.py (full file -- understand high-level ReST pattern, find PPO clip bug),
    src/kicad_agent/training/reward_model.py (lines 360-450 -- find validation loss gap),
    src/kicad_agent/training/sft/trainer.py (around line 189 -- find float16 on MPS),
    src/kicad_agent/training/templates.py (full file -- find always-returns-spatial_reasoning and unused templates),
    src/kicad_agent/training/evaluation.py (lines 200-225 -- find run_ablation stub),
    src/kicad_agent/inference/best_of_n.py (lines 50-60 -- find fake score when reward model unavailable)
  </read_first>
  <action>
    **H-10: Consolidate GRPO implementations**
    First determine which GRPO is active by reading pipeline.py imports. Based on RESEARCH.md insight: `grpo_trainer.py` is likely the higher-level active one used by pipeline, `grpo.py` is the dead lower-level one.
    1. Trace the actual import in pipeline.py to confirm which is used
    2. If grpo.py is dead (not imported by pipeline or any active code), remove it entirely
    3. If grpo_trainer.py is dead, remove it instead
    4. Remove dead KL divergence code from whichever file survives
    5. Do NOT merge the two -- keep the active one, delete the dead one

    **H-11: Fix best_of_n degenerate behavior**
    In `best_of_n.py`, the code returns `chains[0]` with a fake 0.5 score when the reward model is None. Fix this to:
    ```python
    if reward_fn is None:
        raise ValueError("best_of_n_select requires a reward function")
    ```
    Or if there is a legitimate use case for running without reward model, return the first chain with a clear warning and `None` score instead of 0.5.

    **M-14: Fix circular evaluation**
    In `pipeline.py`, find where training data is both trained and evaluated on. Add a proper train/eval split:
    ```python
    from sklearn.model_selection import train_test_split
    # Or manual split:
    split_idx = int(len(data) * 0.8)
    train_data = data[:split_idx]
    eval_data = data[split_idx:]
    ```
    Ensure evaluation only runs on the held-out eval split.

    **M-15: Fix PPO clip**
    In `grpo_trainer.py` (or whichever survives), find the PPO clip that applies to raw advantages. Fix to apply clip to probability ratios:
    ```python
    # Correct PPO: clipped_ratio = clip(ratio, 1-epsilon, 1+epsilon)
    # advantage * clipped_ratio
    ```
    If the code is actually doing advantage clipping (valid in some GRPO variants), update the docstring to say "advantage clipping" instead of "PPO-clip" to avoid confusion.

    **M-16: Add validation loss computation**
    In `reward_model.py`, find the training loop and add validation loss computation at the end of each epoch:
    ```python
    val_loss = self._compute_validation_loss(val_data)
    logger.info("Epoch %d: train_loss=%.4f val_loss=%.4f", epoch, train_loss, val_loss)
    ```

    **M-17: Fix float16 on MPS**
    In `sft/trainer.py`, find where `torch.float16` is used on MPS device. Change to `torch.float32` when device is MPS:
    ```python
    dtype = torch.float32 if str(device) == "mps" else torch.float16
    ```

    **M-18: Remove per-step RNG reset in GRPO**
    In `grpo.py` (whichever survives), find the per-step RNG reset and remove it. Let the global RNG state evolve naturally for exploration diversity.

    **M-19: Implement template selection logic**
    In `templates.py`, find the function that always returns "spatial_reasoning". Implement actual selection logic based on task type:
    ```python
    def select_template(task: str) -> str:
        """Select reasoning template based on task characteristics."""
        task_lower = task.lower()
        if "route" in task_lower or "trace" in task_lower:
            return "routing"
        elif "place" in task_lower or "layout" in task_lower:
            return "placement"
        elif "clearance" in task_lower or "drc" in task_lower:
            return "clearance"
        return "spatial_reasoning"
    ```

    **L-16: Fix or remove run_ablation stub**
    In `evaluation.py`, find `run_ablation` stub. Either implement a basic ablation (compare model with/without specific features) or remove the function entirely.

    **Tests: Create test_training_integrity.py**
    1. Test that pipeline uses train/eval split (no circular evaluation)
    2. Test PPO clip applies to ratios, not advantages (mock values and check)
    3. Test best_of_n raises ValueError when reward_fn is None
    4. Test template selection returns different templates for different task types
    5. Test MPS device uses float32, not float16
  </action>
  <acceptance_criteria>
    - Only one GRPO implementation file remains (dead one removed)
    - Dead KL divergence code removed from surviving GRPO file
    - best_of_n raises ValueError (or returns None score) when reward_fn unavailable
    - pipeline.py uses separate train/eval data splits
    - PPO clip docstring is accurate (either "ratio clipping" or "advantage clipping")
    - reward_model.py logs validation loss per epoch
    - sft/trainer.py uses float32 on MPS device
    - GRPO implementation does NOT reset RNG per step
    - templates.py returns different templates based on task type
    - run_ablation either implemented or removed
    - tests/test_training_integrity.py exists with 5+ test functions
    - All 1479+ existing tests pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python -m pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Training pipeline has no circular evaluation
    - Dead GRPO implementation removed
    - PPO clip mathematically correct with accurate docstring
    - Validation loss computed during reward model training
    - MPS uses float32 to prevent NaN
    - RNG not reset per step
    - Template selection based on task type
    - best_of_n handles missing reward model properly
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix dead repair code and add missing test coverage (H-8, H-12, M-21, M-22)</name>
  <files>
    src/kicad_agent/ops/repair.py,
    tests/test_executor_ops.py,
    tests/test_serializer.py,
    tests/test_e2e_pipeline.py
  </files>
  <read_first>
    src/kicad_agent/ops/repair.py (lines 195-215 for dead net-short detection loop -- the loop body is `pass`),
    src/kicad_agent/ops/executor.py (understand handler registry for add_wire, add_label, add_power),
    src/kicad_agent/handler.py (full file -- understand handle_operation flow),
    src/kicad_agent/serializer/ (list serializer modules -- understand what needs direct tests)
  </read_first>
  <action>
    **H-8: Fix dead repair code in net-short detection**
    In `repair.py`, find the wire propagation loop body that is `pass`. This function detects labels at identical positions but misses the more common case of labels connected by wires. Implement the actual wire propagation logic:
    1. Find all labels in the schematic
    2. For each pair of labels with the same name, check if they are connected via wire segments
    3. Track connectivity through wire junctions and wire endpoints
    4. Return the detected short circuits

    The implementation should follow the existing code patterns in repair.py. Use the SchematicIR API to access wires and labels.

    **M-21: Add executor tests for core schematic ops**
    Create `tests/test_executor_ops.py` with tests for:
    1. `add_wire` -- construct AddWireOp, execute, verify wire appears in schematic
    2. `add_label` -- construct AddLabelOp, execute, verify label appears
    3. `add_power` -- construct AddPowerOp, execute, verify power symbol appears
    4. `add_no_connect` -- construct AddNoConnectOp, execute, verify no-connect marker
    5. `add_junction` -- construct AddJunctionOp, execute, verify junction dot
    Use existing test fixtures (schematic files in tests/fixtures/) if available.

    **M-22: Add serializer unit tests**
    Create `tests/test_serializer.py` with tests for:
    1. Test `serialize_schematic` produces valid output for a known schematic
    2. Test `serialize_pcb` produces valid output for a known PCB
    3. Test `normalize_kicad_output` normalizes whitespace and ordering
    4. Test serializer round-trips correctly (parse -> serialize -> compare)
    Use existing fixture files for test data.

    **H-12: Add end-to-end integration test**
    Create `tests/test_e2e_pipeline.py` with a test that validates the core value proposition:
    ```python
    def test_intent_to_file_pipeline():
        """Test: JSON intent -> Executor -> IR mutation -> Serialize -> File output."""
        # 1. Construct a valid operation JSON (e.g., add_component)
        # 2. Parse via handler.handle_operation()
        # 3. Execute via executor
        # 4. Read the output file
        # 5. Verify the file is valid KiCad format
        # 6. Verify the mutation is reflected in the file
    ```
    Use a fixture schematic file, apply an operation (e.g., move_component), and verify the output file contains the moved component at the new coordinates.
  </action>
  <acceptance_criteria>
    - repair.py net-short detection loop has actual logic (not `pass`)
    - tests/test_executor_ops.py exists with 5+ test functions for core ops
    - tests/test_serializer.py exists with 4+ test functions
    - tests/test_e2e_pipeline.py exists with end-to-end pipeline test
    - All new tests pass
    - All 1479+ existing tests pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bretbouchard/apps/kicad-agent && python -m pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Net-short detection loop has working implementation
    - Core schematic ops (add_wire, add_label, add_power, add_no_connect, add_junction) have executor tests
    - Serializer modules have direct unit tests
    - End-to-end integration test validates: JSON intent -> executor -> IR mutation -> serialize -> file output
    - All existing tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Training data -> Model | Train/eval split prevents information leakage |
| Test -> Production | New tests validate production code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-14 | Information Disclosure | pipeline.py | mitigate | Train/eval split prevents data leakage (M-14) |
| T-24-15 | Tampering | best_of_n.py | mitigate | Explicit error when reward model unavailable (H-11) |
</threat_model>

<verification>
1. All existing tests pass: `python -m pytest tests/ -x -q --tb=short`
2. Training integrity tests pass: `python -m pytest tests/test_training_integrity.py -v`
3. Executor tests pass: `python -m pytest tests/test_executor_ops.py -v`
4. Serializer tests pass: `python -m pytest tests/test_serializer.py -v`
5. E2E test passes: `python -m pytest tests/test_e2e_pipeline.py -v`
</verification>

<success_criteria>
- Training pipeline uses separate train/eval splits (no circular evaluation)
- Dead GRPO implementation removed, active one preserved
- PPO clip mathematically correct
- Validation loss computed during reward model training
- MPS uses float32
- RNG not reset per step
- Template selection based on task type
- best_of_n handles missing reward model properly
- Net-short detection has actual logic
- Core schematic ops have executor tests
- Serializer modules have direct unit tests
- End-to-end integration test validates full pipeline
- All 1479+ existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-council-audit-remediation/24-04-SUMMARY.md`
</output>
