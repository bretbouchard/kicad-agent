__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.kicad_prl
.env
.venv/
venv/
*.egg
.python-version
.claude/
!.claude/CLAUDE.md
.planning/
.benchmarks/
site/
# ERC/DRC reports and staging
*-erc.rpt
*-drc.rpt
.DS_Store
.easyeda_cache/
discovered_repos.json
schema.json
kicad_staging/
/training_data/
training_output/
# Freerouting output (generated DSN/SES during test runs)
tests/fixtures/freerouting/
tests/fixtures/*/freerouting/
# Council IN-04: KiCad + kicad-agent lock files (should never be committed)
*.lck
~$*
.kicad_agent.lock

# Model output (adapters, checkpoints) — stored on /Volumes/Storage
output/
.auto-loop/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Phase 204: Optuna sweep DBs (local-only, resumable per developer)
sweeps/
*.db-journal

# Phase 204: demo artifacts (regenerated per run, not source)
*.png

# Phase 204 / ngspice test artifacts — should never be tracked
*.erc
*.log
__init___sklib.py
demo_closed_box.erc
demo_closed_box.log
generate_legibility_*.erc
generate_legibility_*.log

# Schematic_Editor PDF is 132MB — exceeds GitHub 100MB limit
docs/Schematic_Editor

# Optuna sqlite sweep DBs + outputs (already partially ignored)
/sweeps/
*.db-journal

# Claude settings (per-team-member, not shared)
.claude.json

# KiCad netlist exports (test/run strays)
*.net

# Build artifacts
/kicad_agent-*/
/graphify-out/

# Compiled Python (per-developer)
__pycache__/
*.pyc
