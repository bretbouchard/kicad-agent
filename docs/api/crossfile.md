# Cross-file

Atomic cross-file operations, library propagation, and structural diffs.

The crossfile module handles operations that span multiple KiCad files within a project, such as propagating symbol library changes to schematics or verifying consistency between schematic and PCB representations.

::: volta.crossfile
    options:
      show_source: true
