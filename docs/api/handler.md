# Handler

Operation validation, execution, and result formatting.

The handler module is the primary programmatic entry point. It validates operations against the schema, executes them against target files, and formats results for CLI or API consumers.

::: kicad_agent.handler
    options:
      show_source: true
