// VoltaPCBCore — cross-platform KiCad parsing + validation
// Phase 226: This target will hold the shared code for iOS + macOS.
// Currently the code lives in Volta and is shared via the
// XcodeGen project structure. A future refactor moves the parsing/
// validation/engine code here as a proper library target.
public enum VoltaPCBCoreVersion {
    public static let version = "1.0.0"
    public static let build = "226"
}

